V3 fixes the runtime package size before CloudBase upload.

Why:
- The validated V2 artifact is structurally correct.
- Compressed artifact: ~18.85 MB.
- Expanded runtime: ~58.81 MB.
- Import verification recreated __pycache__ after the old cleanup step.

V3:
- verifies imports on Ubuntu/Python 3.11
- strips unneeded symbols from Linux native shared objects
- verifies imports again after stripping
- removes regenerated bytecode caches and dependency test suites
- removes non-runtime verification/utility root scripts
- hard-fails unless final expanded runtime is below 48 MiB
- uploads fresh folder + zip artifacts

Overlay on cloudbase-p0, commit, and push. The workflow runs automatically.
