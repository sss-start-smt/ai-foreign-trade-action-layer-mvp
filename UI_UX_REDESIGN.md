# UI/UX重构说明

## 视觉原则

- 暖灰页面背景承载长时间工作场景。
- 深绿色导航和关键容器建立稳定、专业的SaaS感。
- 棕金只表示“执行动作”，不承担风险或成功语义。
- 红、蓝、绿、紫分别用于风险、等待、完成、人工确认。
- 浅色背景固定使用深色文字；深色背景固定使用白色或高对比浅色文字。

## UX原则

沟通能力被拆回业务上下文，而不是独立工作台：

```text
订单事实/外部消息
→ AI理解与人工确认
→ 任务进入行动队列
→ 从当前任务生成沟通
→ 人工确认并记录触达
→ 等待回复
→ 收到回复后更新事实与重排
```

### 任务上下文

任务卡根据真实状态展示可用操作：

- `DO_NOW / DO_TODAY`：生成沟通、查看订单、完成或升级。
- `WAITING_EXTERNAL`且未到承诺时间：记录回复，不重复催办。
- `WAITING_EXTERNAL`且已超时：再次催办。
- `NEEDS_CONFIRMATION`：进入AI确认。
- `NOT_MY_RESPONSIBILITY`：转交负责人。

### 订单上下文

订单详情集中展示订单事实、任务、风险、沟通记录和事件时间线；新建任务、录入消息和生成沟通均自动携带当前订单ID。

### 消息上下文

录入消息后先进入FT01/FT02候选审核；用户也可用同一条原文触发FT05，但仍需人工确认后才能写入正式任务。

### 空状态

数据库为空时显示真实空状态和创建/导入入口，不自动出现PO-1001等演示数据。示例数据只能在设置页由用户明确加载，并可彻底清除。
