# Coze三问题合并修复补丁 V4.3

本补丁一次处理：

1. FT01调用返回 `Invalid request parameters`
2. FT02调用返回 `Invalid request parameters`
3. AI确认页点击“确认并调用FT03写回”看起来没有反应

## 覆盖文件

必须覆盖：
- `main.py`
- `coze_integration.py`
- `static/app.js`

建议同步覆盖：
- `tests/test_api.py`
- `tests/test_coze_integration.py`

## 修复点

### FT01 / FT02
- 先用 `parameters` 对象格式调用Coze；
- 若Coze明确拒绝参数格式，自动用JSON字符串格式重试；
- 删除空的可选参数；
- 规范化消息来源：
  - internal/manual → manual_input
  - email → email
  - wechat → wechat
- FT02始终传递合法的 `order_context` JSON字符串；
- 调用日志记录尝试过的参数模式和Coze原始错误。

### 页面反馈
- FT01/FT02按钮显示正在调用、成功或具体错误；
- FT03按钮显示完整写回过程；
- FT03失败时候选仍保持待确认，不伪造成功；
- 前端增加210—240秒超时保护；
- 网页版本升级到 `4.3.0`。

## 部署

1. 上传并覆盖文件；
2. 提交到Render连接的分支；
3. Render：`Manual Deploy → Deploy latest commit`；
4. 浏览器：`Ctrl + F5`；
5. 打开 `/health`，确认 `version` 为 `4.3.0`。

## 测试顺序

1. FT01：客户修改包装；
2. FT02：工厂模糊回复；
3. AI确认：点击FT03写回；
4. 出错时查看 `/api/coze/status` 的 `recent_runs`。

不要发送或截图暴露 `COZE_API_TOKEN`。
