--- /mnt/data/FlowOrder_Frontend_Freeze_Work/original_v6134/FlowOrder_V6.1.3.4_Performance_Work/static/index.html	2026-08-03 14:41:25.000000000 +0000
+++ /mnt/data/FlowOrder_Frontend_Freeze_Work/FlowOrder_V6.1.3.4_Performance_Work/static/index.html	2026-08-04 03:05:03.010846853 +0000
@@ -5,36 +5,175 @@
   <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
   <meta name="theme-color" content="#092923" />
   <meta name="color-scheme" content="light" />
-  <title>AI外贸跟单行动系统</title>
+  <title>FlowOrder · AI外贸跟单行动系统</title>
+  <link rel="preconnect" href="https://fonts.googleapis.com" />
+  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
+  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
   <link rel="stylesheet" href="/static/styles.css?v=6.1.3.4" />
+  <style>
+    .proto-banner{position:sticky;top:0;z-index:300;display:flex;align-items:center;gap:10px;justify-content:center;
+      padding:9px 14px;background:var(--ink);color:#fff;font-size:12px;font-weight:600;letter-spacing:.01em;border-bottom:1px solid var(--line)}
+    .proto-banner b{color:var(--brand)}
+    .metric-grid{grid-template-columns:repeat(4,1fr)}
+    .page-stack{gap:24px}
+    .proto-2col{display:grid;grid-template-columns:1.55fr 1fr;gap:18px;align-items:start}
+    .proto-3col{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;align-items:start}
+    @media(max-width:1080px){.proto-2col,.proto-3col{grid-template-columns:1fr}}
+
+    /* —— 模块分组小标题：细横线 + 大写小字，明确模块边界 —— */
+    .section-title{
+      display:flex;align-items:center;gap:10px;
+      font-size:11px;font-weight:600;letter-spacing:.08em;
+      text-transform:uppercase;color:var(--muted);
+      margin:6px 2px -10px;
+    }
+    .section-title::after{content:"";flex:1;height:1px;background:var(--line)}
+
+    /* —— 面板/卡片边框微调,让模块轮廓更利落 —— */
+    .panel,.order-card,.info-card,.risk-item,.result-card,.review-card,
+    .candidate-row,.integration-item,.context-chip,.meta-item,.channel-option label,
+    .activation-order,.permission-card,.ai-result,.agent-candidate-card,.agent-connection-card,
+    .approval-list>article,.information-gap-list article,
+    .confirm-card,.quick-entry,.session-item{
+      border-color:var(--line-strong);
+    }
+    .mini-chart{width:100%;height:200px;display:block}
+    .row-actions{display:flex;gap:8px;flex-wrap:wrap}
+    .panel .panel-head{display:flex;align-items:center;justify-content:space-between}
+    .order-hero{display:flex;gap:24px;flex-wrap:wrap;align-items:flex-end;margin-top:6px}
+    .order-hero .stat{display:flex;flex-direction:column;gap:2px}
+    .order-hero .stat small{font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;font-weight:600}
+    .order-hero .stat strong{font-size:18px;font-weight:600;font-variant-numeric:tabular-nums}
+    .timeline{list-style:none;margin:0;padding:0}
+    .timeline li{display:flex;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}
+    .timeline li:last-child{border-bottom:none}
+    .timeline .dot{width:9px;height:9px;border-radius:50%;margin-top:5px;background:var(--brand);flex:none}
+    .timeline .dot.muted{background:var(--muted-2)}
+    .demo-note{font-size:11px;color:var(--muted)}
+
+    /* —— 今日工作台：快捷入口 —— */
+    .quick-entries{display:flex;gap:10px;flex-wrap:wrap;margin:2px 0 4px}
+    .quick-entry{display:flex;align-items:center;gap:8px;padding:10px 14px;border:1px solid var(--line);border-radius:10px;background:#fff;font-size:13px;font-weight:500;color:var(--ink);cursor:pointer;transition:border-color .15s var(--ease)}
+    .quick-entry:hover{border-color:var(--line-strong)}
+    .quick-entry .metric-icon{width:28px;height:28px;border-radius:8px;background:var(--surface-2);display:inline-flex;align-items:center;justify-content:center}
+
+    /* —— Agent 助手：三栏 —— */
+    .agent-3col{display:grid;grid-template-columns:230px 1fr 1fr;gap:18px;align-items:stretch;min-height:560px}
+    @media(max-width:1080px){.agent-3col{grid-template-columns:1fr}}
+    .session-list{display:flex;flex-direction:column;gap:8px}
+    .session-item{padding:11px 12px;border:1px solid var(--line);border-radius:10px;background:#fff;cursor:pointer;text-align:left}
+    .session-item:hover{border-color:var(--line-strong)}
+    .session-item.active{border-color:var(--brand);background:var(--brand-soft)}
+    .session-item .s-when{font-size:10.5px;color:var(--muted);text-transform:uppercase;letter-spacing:.04em;font-weight:600}
+    .session-item .s-title{font-size:13px;font-weight:500;color:var(--ink);margin-top:3px}
+    .chat{display:flex;flex-direction:column;gap:12px;flex:1;min-height:0}
+    .chat-bubble{max-width:86%;padding:10px 13px;border-radius:12px;font-size:13px;line-height:1.55}
+    .chat-bubble.user{align-self:flex-end;background:var(--brand);color:#fff;border-bottom-right-radius:4px}
+    .chat-bubble.agent{align-self:flex-start;background:var(--surface-2);color:var(--ink);border:1px solid var(--line);border-bottom-left-radius:4px}
+    .chat-bubble .c-time{display:block;font-size:10.5px;opacity:.6;margin-top:4px}
+    .chat-input{display:flex;gap:8px;margin-top:14px}
+    .chat-input input{flex:1;height:38px;border:1px solid var(--line);border-radius:10px;padding:0 12px;font-size:13px}
+    .chat-input input:focus{outline:none;border-color:var(--brand)}
+    .run-row{display:flex;gap:10px;padding:10px 0;border-bottom:1px solid var(--line);font-size:13px}
+    .run-row:last-child{border-bottom:none}
+    .run-row .r-label{width:84px;flex:none;color:var(--muted);font-weight:500}
+    .run-row .r-val{flex:1}
+    .trace{list-style:none;margin:0;padding:0;font-size:12.5px}
+    .trace li{display:flex;gap:8px;padding:6px 0;color:var(--ink-2)}
+    .trace li::before{content:"›";color:var(--brand);font-weight:700}
+
+    /* —— 标签/来源 —— */
+    .src-tag{display:inline-flex;align-items:center;padding:2px 8px;border-radius:6px;font-size:11px;font-weight:600}
+    .src-tag.manual{background:var(--surface-3);color:var(--muted)}
+    .src-tag.msg{background:var(--accent-soft);color:var(--accent)}
+    .src-tag.agent{background:var(--brand-soft);color:var(--brand-700)}
+    .src-tag.wait{background:var(--amber-soft);color:var(--amber)}
+
+    /* —— 订单详情页签 —— */
+    .tabs{display:flex;gap:4px;padding:0 20px;border-bottom:1px solid var(--line);background:#fff}
+    .tab{padding:12px 14px;font-size:13px;font-weight:500;color:var(--muted);border-bottom:2px solid transparent;cursor:pointer;background:none;border-top:none;border-left:none;border-right:none}
+    .tab:hover{color:var(--ink)}
+    .tab.active{color:var(--brand-700);border-bottom-color:var(--brand);font-weight:600}
+    .tab-panel{display:none}
+    .tab-panel.active{display:block}
+
+    /* —— 确认中心卡片 —— */
+    .confirm-list{display:flex;flex-direction:column;gap:14px}
+    .confirm-card{border:1px solid var(--line);border-radius:var(--radius);padding:16px 18px;background:#fff}
+    .confirm-card .cc-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}
+    .cc-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px 20px;font-size:12.5px}
+    .cc-grid .cc-k{color:var(--muted);font-weight:500}
+    .cc-grid .cc-v{color:var(--ink)}
+    .cc-evidence{margin-top:10px;padding:10px 12px;background:var(--surface-2);border-radius:8px;font-size:12.5px;color:var(--ink-2);border-left:3px solid var(--line-strong)}
+
+    /* —— 消息中心流程 —— */
+    .flow{display:flex;align-items:center;gap:6px;flex-wrap:wrap;margin:10px 0 16px;font-size:12px;color:var(--muted)}
+    .flow .step{display:inline-flex;align-items:center;gap:5px;padding:5px 10px;border:1px solid var(--line);border-radius:999px;background:#fff}
+    .flow .arrow{color:var(--muted-2)}
+    .src-pick{display:flex;gap:8px;flex-wrap:wrap;margin:8px 0 14px}
+
+    /* —— V3 借鉴：状态 / 运行摘要 / 技术详情 / 筛选 等功能性组件（沿用本稿视觉 token）—— */
+    .status-stack{display:flex;flex-direction:column;gap:6px;width:100%;font-size:11px}
+    .status-line{display:grid;grid-template-columns:8px 1fr auto;gap:7px;align-items:center;color:var(--muted)}
+    .status-line b{font-size:11px;color:var(--ink);font-weight:600}
+    .status-dot{width:7px;height:7px;border-radius:50%;background:var(--muted-2)}
+    .status-dot.ok{background:var(--green)}
+    .status-dot.warn{background:var(--amber)}
+
+    .session-item .s-meta{display:flex;gap:6px;flex-wrap:wrap;margin-top:6px;font-size:10.5px;color:var(--muted)}
+    .session-item .s-meta span{display:inline-flex;align-items:center;padding:2px 6px;border-radius:999px;background:var(--surface-2)}
+
+    .agent-run-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:12px}
+    .agent-run-summary .summary-cell{padding:12px;border:1px solid var(--line);border-radius:10px;background:var(--surface-2)}
+    .agent-run-summary small{display:block;color:var(--muted);font-size:10.5px;margin-bottom:4px;font-weight:600;text-transform:uppercase;letter-spacing:.04em}
+    .agent-run-summary strong{font-size:18px;font-variant-numeric:tabular-nums;color:var(--ink)}
+
+    .tech-details{margin-top:12px;border-top:1px solid var(--line);padding-top:10px}
+    .tech-details summary{cursor:pointer;color:var(--muted);font-size:12px;font-weight:600;list-style:none}
+    .tech-details summary::-webkit-details-marker{display:none}
+    .tech-box{margin-top:8px;padding:10px 12px;border-radius:8px;background:var(--surface-2);font-size:11.5px;color:var(--ink-2);line-height:1.7;word-break:break-word}
+
+    .confirm-filters{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px}
+    .confirm-filter{height:30px;padding:0 11px;border:1px solid var(--line);border-radius:8px;background:#fff;color:var(--muted);font-size:12px;cursor:pointer}
+    .confirm-filter.active{border-color:var(--brand);background:var(--brand-soft);color:var(--brand-700);font-weight:600}
+
+    .business-note{margin-top:10px;padding:10px 12px;border-radius:8px;background:var(--amber-soft);color:var(--ink-2);font-size:12px;line-height:1.6}
+
+    /* —— 导航项图标：data-icon 移到独立 .icon 容器，避免覆盖文字 —— */
+    .nav-item .icon{display:grid;place-items:center;flex:0 0 18px;width:18px;height:18px}
+    .nav-item .icon svg{width:100%;height:100%}
+  </style>
 </head>
 <body>
   <div class="app-shell">
     <aside class="sidebar" id="sidebar">
-      <button class="brand" data-route="today" aria-label="返回今日行动">
+      <button class="brand" data-route="today" aria-label="返回今日工作台">
         <span class="brand-mark" aria-hidden="true"><i></i><i></i><i></i></span>
         <span class="brand-copy"><strong>FlowOrder</strong><small>AI外贸跟单行动系统</small></span>
       </button>
 
       <div class="nav-section-label">工作空间</div>
       <nav class="side-nav" aria-label="主导航">
-        <button class="nav-item" data-route="today" data-icon="today"><span>今日行动</span><b id="todayBadge" hidden>0</b></button>
-        <button class="nav-item" data-route="agent" data-icon="agent"><span>异常诊断</span><b id="agentBadge" hidden>0</b></button>
-        <button class="nav-item" data-route="tasks" data-icon="tasks"><span>全部任务</span></button>
-        <button class="nav-item" data-route="orders" data-icon="orders"><span>订单中心</span></button>
-        <button class="nav-item" data-route="intake" data-icon="message"><span>消息接入</span></button>
-        <button class="nav-item" data-route="confirm" data-icon="review"><span>人工确认</span><b id="reviewBadge" hidden>0</b></button>
+        <button class="nav-item" data-route="today"><span class="icon" data-icon="today"></span><span>今日工作台</span><b id="todayBadge" hidden>0</b></button>
+        <button class="nav-item" data-route="agent"><span class="icon" data-icon="agent"></span><span>Agent助手</span></button>
+        <button class="nav-item" data-route="tasks"><span class="icon" data-icon="tasks"></span><span>任务中心</span></button>
+        <button class="nav-item" data-route="orders"><span class="icon" data-icon="orders"></span><span>订单中心</span></button>
+        <button class="nav-item" data-route="intake"><span class="icon" data-icon="message"></span><span>消息中心</span></button>
+        <button class="nav-item" data-route="confirm"><span class="icon" data-icon="review"></span><span>确认中心</span><b id="reviewBadge" hidden>0</b></button>
       </nav>
 
-      <div class="nav-section-label">管理</div>
+      <div class="nav-section-label">管理 · 仅主管</div>
       <nav class="side-nav" aria-label="管理导航">
-        <button class="nav-item" data-route="manage" data-icon="chart"><span>管理看板</span></button>
-        <button class="nav-item" data-route="settings" data-icon="settings"><span>设置与连接</span></button>
+        <button class="nav-item" data-route="manage"><span class="icon" data-icon="chart"></span><span>管理看板</span></button>
+        <button class="nav-item" data-route="settings"><span class="icon" data-icon="settings"></span><span>设置与连接</span></button>
       </nav>
 
-      <div class="sidebar-card">
-        <span class="live-dot" id="sidebarLiveDot"></span>
-        <div><strong id="sidebarStatus">服务连接中</strong><small id="sidebarStatusDetail">正在检查自动化连接</small></div>
+      <div class="sidebar-card" style="align-items:flex-start">
+        <div class="status-stack" aria-label="系统运行状态">
+          <div class="status-line"><span class="status-dot" id="systemStatusDot"></span><span>系统服务</span><b id="systemStatusValue">检查中</b></div>
+          <div class="status-line"><span class="status-dot" id="agentStatusDot"></span><span>Agent 服务</span><b id="agentStatusValue">检查中</b></div>
+          <div class="status-line"><span class="status-dot" id="scheduleStatusDot"></span><span>定时巡检</span><b id="scheduleStatusValue">检查中</b></div>
+        </div>
       </div>
     </aside>
 
@@ -43,21 +182,20 @@
         <div class="topbar-left">
           <button class="mobile-menu" id="mobileMenu" aria-label="打开导航" data-icon="menu"></button>
           <div class="page-heading">
-            <span id="pageEyebrow">ACTION WORKSPACE</span>
-            <h1 id="pageTitle">今日行动</h1>
-            <p id="pageSubtitle">从订单事实出发，完成下一步行动</p>
+            <span id="pageEyebrow">TODAY WORKBENCH</span>
+            <h1 id="pageTitle">今日工作台</h1>
           </div>
         </div>
         <div class="topbar-actions">
           <label class="global-search" id="searchWrap">
             <span data-icon="search" aria-hidden="true"></span>
-            <input id="globalSearch" type="search" autocomplete="off" placeholder="搜索当前页面" aria-label="搜索当前页面" />
+            <input id="globalSearch" type="search" autocomplete="off" placeholder="搜索订单、任务、客户或会话" aria-label="全局搜索" />
             <kbd>⌘ K</kbd>
           </label>
           <button class="icon-btn" id="refreshBtn" title="刷新页面" data-icon="refresh"></button>
           <button class="profile" id="profileButton" type="button">
             <span class="avatar" id="profileAvatar">李</span>
-            <span><strong id="profileName">李梅</strong><small id="profileRole">跟单专员</small></span>
+            <span><strong id="profileName">李梅</strong><small id="profileRole">跟单专员 · 演示</small></span>
             <span data-icon="chevron"></span>
           </button>
         </div>
@@ -70,17 +208,15 @@
   </div>
 
   <nav class="mobile-nav" aria-label="移动端导航">
-    <button data-route="today" data-icon="today"><small>行动</small></button>
+    <button data-route="today" data-icon="today"><small>工作台</small></button>
+    <button data-route="agent" data-icon="agent"><small>Agent</small></button>
     <button data-route="tasks" data-icon="tasks"><small>任务</small></button>
     <button data-route="orders" data-icon="orders"><small>订单</small></button>
-    <button data-route="intake" data-icon="message"><small>接入</small></button>
     <button data-route="confirm" data-icon="review"><small>确认</small></button>
   </nav>
 
   <div class="drawer-mask" id="drawerMask" hidden></div>
-  <aside class="drawer" id="drawer" aria-hidden="true" aria-label="详情面板">
-    <div id="drawerBody"></div>
-  </aside>
+  <aside class="drawer" id="drawer" aria-hidden="true" aria-label="详情面板"><div id="drawerBody"></div></aside>
 
   <dialog class="modal" id="modal">
     <form method="dialog" id="modalForm">
@@ -94,6 +230,6 @@
   </dialog>
 
   <div class="toast-region" id="toastRegion" aria-live="polite" aria-atomic="true"></div>
-  <script src="/static/app.js?v=6.1.3.4"></script>
+  <script src="/static/app.js?v=6.1.3.5-ui-freeze"></script>
 </body>
 </html>
--- /mnt/data/FlowOrder_Frontend_Freeze_Work/original_v6134/FlowOrder_V6.1.3.4_Performance_Work/static/app.js	2026-08-03 14:45:48.000000000 +0000
+++ /mnt/data/FlowOrder_Frontend_Freeze_Work/FlowOrder_V6.1.3.4_Performance_Work/static/app.js	2026-08-04 03:01:40.050851254 +0000
@@ -49,15 +49,15 @@
 const contactStatusLabels={NOT_CONTACTED:'尚未联系',WAITING_REPLY:'已联系，等待回复',REPLIED:'已收到回复',UNKNOWN:'不清楚'};
 const draftTypeLabels={CUSTOMER_REPLY:'客户回复',CUSTOMER_CONFIRMATION_REMINDER:'催客户确认',SUPPLIER_PROGRESS_FOLLOWUP:'催工厂进度',DELIVERY_STATUS_REPLY:'回复交期状态',CHANGE_HISTORY_SUMMARY:'汇总客户变更'};
 const pageMeta={
-  agent:['AGENT DIAGNOSIS','异常诊断','自主检索证据、生成异常候选并等待人工确认'],
-  today:['ACTION WORKSPACE','今日行动','根据真实订单状态、等待窗口和风险排序下一步'],
-  tasks:['TASK OPERATIONS','全部任务','查看、分派、执行和关闭所有真实任务'],
-  orders:['ORDER CONTROL','订单中心','管理订单事实、风险、任务与沟通记录'],
-  activation:['FIRST VALUE','首次行动初始化','从原始订单底座补充动态信息，生成第一条行动和跨订单排序'],
-  intake:['MESSAGE INTAKE','消息接入','录入任意客户或工厂消息，并进入人工确认流程'],
-  confirm:['HUMAN REVIEW','人工确认','系统只提供候选；正式写回必须由人确认'],
-  manage:['MANAGEMENT VIEW','管理看板','查看工作负载、等待分布和风险聚合'],
-  settings:['SYSTEM CONTROL','身份与设置','切换演示身份；跟单人员只看本人订单，主管查看团队数据']
+  today:['TODAY WORKBENCH','今日工作台'],
+  agent:['AGENT ASSISTANT','Agent助手'],
+  tasks:['TASK CENTER','任务中心'],
+  orders:['ORDER CENTER','订单中心'],
+  activation:['FIRST VALUE','首次行动初始化'],
+  intake:['MESSAGE CENTER','消息中心'],
+  confirm:['CONFIRMATION HUB','确认中心'],
+  manage:['MANAGEMENT VIEW','管理看板'],
+  settings:['SYSTEM SETTINGS','设置与连接']
 };
 let route={name:'today',id:null,query:{}};
 let settings={theme:'upstream',compact:false,current_user_id:'USER-1',notifications:{}};
@@ -104,7 +104,7 @@
 function parseRoute(){const raw=location.hash.replace(/^#/,'')||'today';const [path,q='']=raw.split('?');const parts=path.split('/').filter(Boolean);return{name:parts[0]||'today',id:parts[1]||null,query:Object.fromEntries(new URLSearchParams(q))}}
 function go(path){location.hash=path}
 function bindRouteButtons(root=document){$$('[data-route]',root).forEach(b=>b.onclick=()=>{go(b.dataset.route);$('#sidebar').classList.remove('open')});$$('[data-go]',root).forEach(b=>b.onclick=()=>go(b.dataset.go));$$('[data-metric-go]',root).forEach(card=>{const open=()=>go(card.dataset.metricGo);card.onclick=open;card.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open()}}})}
-function setPageMeta(name){const meta=pageMeta[name]||pageMeta.today;$('#pageEyebrow').textContent=meta[0];$('#pageTitle').textContent=meta[1];$('#pageSubtitle').textContent=meta[2];$$('[data-route]').forEach(b=>b.classList.toggle('active',b.dataset.route===name));$('#globalSearch').placeholder=name==='orders'?'搜索订单、客户、产品或节点':name==='tasks'||name==='today'?'搜索任务、订单或客户':'搜索当前页面'}
+function setPageMeta(name){const meta=pageMeta[name]||pageMeta.today;$('#pageEyebrow').textContent=meta[0];$('#pageTitle').textContent=meta[1];$$('[data-route]').forEach(b=>b.classList.toggle('active',b.dataset.route===name||(name==='orders'&&route.id&&b.dataset.route==='orders')));$('#globalSearch').placeholder='搜索订单、任务、客户或会话'}
 function matchTask(t){if(!search)return true;const o=t.order||{};return [t.title,t.recommended_action,t.action_state,o.order_no,o.customer_name,o.product_name].join(' ').toLowerCase().includes(search.toLowerCase())}
 function matchOrder(o){if(!search)return true;return [o.order_no,o.customer_name,o.product_name,o.current_node,o.factory_name].join(' ').toLowerCase().includes(search.toLowerCase())}
 function emptyState(title,desc,actions=''){return `<div class="empty-state"><div class="empty-icon">${icon('inbox')}</div><h3>${esc(title)}</h3><p>${esc(desc)}</p>${actions?`<div class="empty-actions">${actions}</div>`:''}</div>`}
@@ -150,14 +150,13 @@
     }
   });
 }
-function updateProfile(){const op=currentOperator();$('#profileName').textContent=op.name;$('#profileRole').textContent=op.role;$('#profileAvatar').textContent=op.name.slice(0,1);$$('[data-route="manage"]').forEach(el=>el.hidden=op.user_id!=='MANAGER-1');const settingsLabel=$('[data-route="settings"] span');if(settingsLabel)settingsLabel.textContent=op.user_id==='MANAGER-1'?'设置与连接':'身份与设置';document.body.dataset.role=op.user_id==='MANAGER-1'?'manager':'operator'}
-function updateHealth(h,error=''){
-  const dot=$('#sidebarLiveDot');dot.className='live-dot';
-  if(!h){dot.classList.add('bad');$('#sidebarStatus').textContent='后端服务异常';$('#sidebarStatusDetail').textContent=error||'请查看Railway部署日志';return}
-  const agentConfigured=!!h.agent?.configured;
-  dot.classList.toggle('warn',!agentConfigured);
-  $('#sidebarStatus').textContent='后端服务正常';
-  $('#sidebarStatusDetail').textContent=`v${h.version} · Coze Agent${agentConfigured?'已配置':'未配置'}`;
+function updateProfile(){const op=currentOperator();$('#profileName').textContent=op.name;$('#profileRole').textContent=`${op.role} · 演示`;$('#profileAvatar').textContent=op.name.slice(0,1);$$('[data-route="manage"]').forEach(el=>el.hidden=op.user_id!=='MANAGER-1');const settingsLabel=$('[data-route="settings"] span:last-of-type');if(settingsLabel)settingsLabel.textContent=op.user_id==='MANAGER-1'?'设置与连接':'身份与设置';document.body.dataset.role=op.user_id==='MANAGER-1'?'manager':'operator'}
+async function updateHealth(h,error=''){
+  const set=(dot,value,tone,text)=>{if(dot){dot.className=`status-dot ${tone||''}`.trim()}if(value)value.textContent=text};
+  const systemDot=$('#systemStatusDot'),systemValue=$('#systemStatusValue'),agentDot=$('#agentStatusDot'),agentValue=$('#agentStatusValue'),scheduleDot=$('#scheduleStatusDot'),scheduleValue=$('#scheduleStatusValue');
+  if(!h){set(systemDot,systemValue,'','异常');set(agentDot,agentValue,'','未知');set(scheduleDot,scheduleValue,'','未知');return}
+  set(systemDot,systemValue,'ok','在线');
+  try{const s=await api('/api/agent/status',{timeoutMs:12000});set(agentDot,agentValue,s.coze_agent?.configured?'ok':'warn',s.coze_agent?.configured?'已配置':'未配置');set(scheduleDot,scheduleValue,s.daily_schedule?.scheduler_verified?'ok':'warn',s.daily_schedule?.scheduler_verified?'已验证':s.daily_schedule?.configured?'待验证':'未配置')}catch{set(agentDot,agentValue,h.agent?.configured?'ok':'warn',h.agent?.configured?'已配置':'未配置');set(scheduleDot,scheduleValue,'warn','待验证')}
 }
 function updateBadges(todayCount,reviewCount,agentCount=null){if(todayCount!=null){$('#todayBadge').hidden=!todayCount;$('#todayBadge').textContent=todayCount}if(reviewCount!=null){$('#reviewBadge').hidden=!reviewCount;$('#reviewBadge').textContent=reviewCount}if(agentCount!=null&&$('#agentBadge')){$('#agentBadge').hidden=!agentCount;$('#agentBadge').textContent=agentCount}}
 async function renderRoute(resetSearch=true,preserveSearchFocus=false){
@@ -171,6 +170,43 @@
 async function dashboardData(){return cache.dashboard||(cache.dashboard=await api(`/api/dashboard?current_user_id=${encodeURIComponent(currentUser())}`))}
 async function ordersData(){return cache.orders||(cache.orders=await api(`/api/orders?current_user_id=${encodeURIComponent(currentUser())}`))}
 
+
+function uiMetric(label,value,color,ic,delta='',up=false,goTo=''){
+  return `<div class="metric-card ${color}"${goTo?` data-go="${esc(goTo)}" role="button" tabindex="0"`:''}><span class="metric-icon">${icon(ic)}</span><div class="metric-body"><span class="label">${esc(label)}</span><div class="metric-value-row"><strong>${Number(value||0)}</strong>${delta?`<span class="delta ${up?'up':'down'}">${up?'▲':'▼'} ${esc(delta)}</span>`:''}</div></div></div>`
+}
+function uiActionRow(t,buttonLabel='处理'){
+  const o=t.order||{};const tone=['DO_NOW','ESCALATE'].includes(t.action_state)?'red':t.action_state==='DO_TODAY'?'amber':t.action_state==='WAITING_EXTERNAL'?'blue':'muted';const label=stateLabels[t.action_state]||'待处理';const due=t.promised_reply_at||t.next_action_at||t.business_deadline;
+  return `<div class="action-row" style="display:flex;gap:14px;align-items:center;padding:14px 0;border-bottom:1px solid var(--line)"><span class="metric-icon" style="width:34px;height:34px;border-radius:9px;background:var(--surface-2);display:inline-flex;align-items:center;justify-content:center;color:var(--ink-2)">${icon(t.action_state==='WAITING_EXTERNAL'?'clock':'tasks')}</span><div style="flex:1"><strong style="font-size:13.5px">${esc(t.title||'未命名任务')}</strong><br><small class="demo-note">${esc(o.order_no||'未关联订单')}${o.customer_name?` · ${esc(o.customer_name)}`:''}</small></div><span class="tag ${tone}">${esc(label)}${due?` · ${esc(relative(due))}`:''}</span><button class="btn link" data-task-detail="${esc(t.task_id)}">${esc(buttonLabel)}</button></div>`
+}
+function todayReviewRow(x){return `<div class="action-row" style="display:flex;gap:14px;align-items:center;padding:14px 0;border-bottom:1px solid var(--line)"><span class="metric-icon" style="width:34px;height:34px;border-radius:9px;background:var(--surface-2);display:inline-flex;align-items:center;justify-content:center;color:var(--ink-2)">${icon('review')}</span><div style="flex:1"><strong style="font-size:13.5px">${esc(x.order_no||'待关联订单')} · 消息变化候选</strong><br><small class="demo-note">${esc((x.raw_content||'').slice(0,52))}</small></div><button class="btn link" data-go="confirm?review=${esc(x.review_id)}">查看</button></div>`}
+function firstLoginPanel(){return `<section class="panel"><div class="panel-head"><h3>先建立订单底座</h3><span class="tag muted">首次进入</span></div><div class="panel-body"><p>FlowOrder先保存客户、产品、数量、客户正式交期和负责人等订单事实。导入后再补充当前进度或最新沟通，系统才生成行动排序。</p><div class="flow"><span class="step">① 导入订单底座</span><span class="arrow">→</span><span class="step">② 补充动态进展</span><span class="arrow">→</span><span class="step">③ 生成今日行动</span></div><div class="row-actions"><a class="btn primary" href="/import-orders">批量导入订单</a><a class="btn secondary" href="/api/import/template.xlsx">下载Excel模板</a><button class="btn ghost" id="firstNewOrder">新建一笔订单</button></div></div></section>`}
+function taskSourceInfo(t){const text=String(t.source||t.source_type||t.created_by||'').toUpperCase();if(text.includes('AGENT'))return{t:'Agent巡检生成',c:'agent'};if(t.source_message_id||text.includes('MESSAGE')||text.includes('FT0'))return{t:'消息生成',c:'msg'};if(t.action_state==='WAITING_EXTERNAL'||text.includes('WAIT'))return{t:'等待恢复',c:'wait'};return{t:'人工创建',c:'manual'}}
+function taskTableRow(t){const src=taskSourceInfo(t);const due=t.next_action_at||t.business_deadline||t.promised_reply_at;return `<tr><td><strong>${esc(t.title||'未命名任务')}</strong><br><small class="demo-note">${esc(t.order?.order_no||'未关联订单')}</small></td><td><span class="src-tag ${src.c}">${src.t}</span></td><td>${esc(operatorNames[t.owner_user_id]||t.owner_user_id||'未分配')}</td><td>${esc(fdt(due))}</td><td>${esc(t.waiting_on||'—')}</td><td>${statusBadge(t.action_state)}</td><td><div class="row-actions">${taskActions(t,true)}<button class="btn link" data-task-detail="${esc(t.task_id)}">打开</button></div></td></tr>`}
+function orderTableRow(o){return `<tr><td><strong>${esc(o.order_no)}</strong><br><small class="demo-note">${esc(o.customer_name||'未知客户')}</small></td><td>${esc(o.current_node||'待补充')}</td><td>${esc(operatorNames[o.owner]||o.owner||'未分配')}</td><td>${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</td><td>${riskBadge(o.max_risk||'none')}</td><td>${esc(readinessNextAction(o))}</td><td>${Number(o.open_task_count||0)}</td><td><button class="btn link" data-order-detail="${esc(o.order_id)}">详情</button></td></tr>`}
+function orderTaskRow(t){const src=taskSourceInfo(t);return `<tr><td><strong>${esc(t.title||'未命名任务')}</strong></td><td><span class="src-tag ${src.c}">${src.t}</span></td><td>${esc(fdt(t.next_action_at||t.business_deadline||t.promised_reply_at))}</td><td>${statusBadge(t.action_state)}</td></tr>`}
+
+function agentStoreKey(){return `floworderAgentConversations:${currentUser()}`}
+function agentActiveKey(){return `floworderAgentActive:${currentUser()}`}
+function newAgentConversation(){return{id:`LCONV-${Date.now()}-${Math.random().toString(16).slice(2,7)}`,title:'新会话',created_at:new Date().toISOString(),updated_at:new Date().toISOString(),messages:[],coze_conversation_id:null,last_job_id:null,last_run_id:null,metrics:null}}
+function loadAgentConversations(){try{const v=JSON.parse(localGet(agentStoreKey())||'[]');return Array.isArray(v)?v:[]}catch{return[]}}
+function saveAgentConversations(list){localSet(agentStoreKey(),JSON.stringify(list.slice(0,20).map(c=>({...c,messages:(c.messages||[]).slice(-12)}))))}
+function saveOneConversation(conv){const list=loadAgentConversations();const i=list.findIndex(x=>x.id===conv.id);if(i>=0)list[i]=conv;else list.unshift(conv);list.sort((a,b)=>String(b.updated_at).localeCompare(String(a.updated_at)));saveAgentConversations(list)}
+function agentSessionItem(c,active){const meta=[];if(c.last_run_id)meta.push('已运行');if(c.metrics?.risk!=null)meta.push(`${c.metrics.risk}笔风险`);if(!meta.length)meta.push('未开始');return `<button class="session-item ${active?'active':''}" data-agent-session="${esc(c.id)}"><div class="s-when">${esc(fdt(c.updated_at))}</div><div class="s-title">${esc(c.title||'新会话')}</div><div class="s-meta">${meta.map(x=>`<span>${esc(x)}</span>`).join('')}</div></button>`}
+function agentMessagesHtml(messages=[]){if(!messages.length)return '<p class="demo-note">输入一个业务目标开始新会话。系统会保留本次会话ID，后续追问可以继续引用上一轮结果。</p>';return messages.map(m=>`<div class="chat-bubble ${m.role==='user'?'user':'agent'}">${esc(m.content)}<span class="c-time">${esc(fdt(m.time))}</span></div>`).join('')}
+function toolDisplayName(name){return({diagnose_priority_orders:'分析订单风险并排序',create_task_draft:'生成任务草稿',create_approval_request:'创建人工审批',deterministic_rule_inspection:'执行规则巡检',backend_finalize_agent_run:'保存运行结果',start_agent_run:'创建运行记录',complete_agent_run:'完成运行记录'}[name]||name)}
+function agentStructuredResultHtml({active,job,latest,latestRun,latestCalls,status,metrics}){const answer=job?.result?.answer||(active.messages||[]).filter(x=>x.role==='assistant').slice(-1)[0]?.content||'';const calls=latestCalls||[];return `<div class="run-row"><span class="r-label">扫描范围</span><span class="r-val">未来14天 · ${Number(metrics.screened||0)} 笔订单</span></div><div class="run-row"><span class="r-label">风险订单</span><span class="r-val">${Number(metrics.risk||0)} 笔，不足7笔不补齐</span></div><div class="run-row"><span class="r-label">信息缺口</span><span class="r-val">${Number(metrics.gaps||0)} 笔，未计入风险排序</span></div><div class="run-row"><span class="r-label">执行过程</span><span class="r-val"><ul class="trace">${calls.map(c=>`<li>${esc(toolDisplayName(c.tool_name))}${c.duration_ms!=null?` · ${Number(c.duration_ms)}ms`:''}</li>`).join('')||'<li>尚无执行轨迹</li>'}</ul></span></div><div class="run-row"><span class="r-label">停止原因</span><span class="r-val">${esc(latestRun?.stop_reason||latestRun?.status||job?.status||'尚未运行')}</span></div>${answer?`<div class="cc-evidence">${esc(answer)}</div>`:''}<details class="tech-details"><summary>查看技术执行详情</summary><div class="tech-box">run_id：${esc(active.last_run_id||latestRun?.run_id||'—')}<br>conversation_id：${esc(active.coze_conversation_id||job?.conversation_id||job?.result?.conversation_id||'—')}<br>Agent状态：${status.coze_agent?.configured?'已配置':'未配置'}<br>工具调用：${calls.map(c=>esc(c.tool_name)).join(' → ')||'—'}</div></details><div class="row-actions" style="margin-top:12px"><button class="btn secondary" id="runRuleInspection">仅运行规则巡检</button><button class="btn primary" data-go="confirm">查看确认事项</button></div>`}
+async function pollConversationJob(root,conversationId,jobId,attempt=0){if(!root.isConnected||route.name!=='agent')return;const conv=loadAgentConversations().find(x=>x.id===conversationId);if(!conv)return;const status=$('#agentLiveStatus',root),button=$('#askAgent',root);try{const job=await api(`/api/agent/chat/jobs/${encodeURIComponent(jobId)}`,{timeoutMs:15000});if(job.status==='COMPLETED'){const answer=job.result?.answer||'Agent已完成运行，请查看右侧结构化结果。';conv.messages.push({role:'assistant',content:answer,time:new Date().toISOString()});conv.coze_conversation_id=job.conversation_id||job.result?.conversation_id||conv.coze_conversation_id;conv.last_run_id=job.linked_run_id||job.result?.run?.run_id||conv.last_run_id;conv.last_processed_job_id=job.job_id;conv.updated_at=new Date().toISOString();saveOneConversation(conv);toast('Agent运行完成','success');renderRoute(false);return}if(job.status==='FAILED'){status.textContent=job.error_message||'Agent运行未完成';button.disabled=false;toast(status.textContent,'error');return}status.textContent=`${job.message||'Agent正在后台执行'} · 已等待 ${Math.round((Date.now()-new Date(job.created_at).getTime())/1000)} 秒`;button.disabled=true;setTimeout(()=>pollConversationJob(root,conversationId,jobId,attempt+1),attempt<15?1800:3000)}catch(e){status.textContent='正在恢复后台任务状态…';if(attempt<60)setTimeout(()=>pollConversationJob(root,conversationId,jobId,attempt+1),3000);else button.disabled=false}}
+
+function confirmCardShell(category,type,ref,suggest,evidence,fields,risk,by,after,actions,color='muted'){return `<div class="confirm-card" data-confirm-type="${esc(category)}"><div class="cc-head"><span class="tag ${color}">${esc(type)}</span><span class="demo-note">${esc(ref||'')}</span></div><div class="cc-grid"><span class="cc-k">系统建议</span><span class="cc-v">${esc(suggest||'—')}</span><span class="cc-k">影响字段</span><span class="cc-v">${esc(fields||'—')}</span><span class="cc-k">风险</span><span class="cc-v">${esc(risk||'—')}</span><span class="cc-k">发起方</span><span class="cc-v">${esc(by||'—')}</span><span class="cc-k">确认后</span><span class="cc-v">${esc(after||'—')}</span></div><div class="cc-evidence">${esc(evidence||'暂无证据摘要')}</div><div class="row-actions" style="margin-top:12px">${actions}</div></div>`}
+function confirmReviewCard(x){const c=x.candidate||safeJson(x.candidate_json,{});const fields=(c.fields||[]).map(f=>fieldLabel(f.field_name)).join('、')||'订单字段候选';const suggest=(c.fields||[]).map(f=>`${fieldLabel(f.field_name)}：${f.normalized_value??'—'}`).join('；')||'查看AI提取的字段变化';const risk=(c.risk_signals||[])[0]?.risk_level||'需人工确认';const actions=x.status==='PENDING'?`<button class="btn primary" data-review-confirm="${esc(x.review_id)}">确认</button><button class="btn ghost" data-review-reject="${esc(x.review_id)}">驳回</button><button class="btn link" data-review-open="${esc(x.review_id)}">查看详情</button>`:`<button class="btn link" data-review-open="${esc(x.review_id)}">查看详情</button>`;return confirmCardShell('数据变更','消息字段变更',x.order_no||x.review_id,suggest,x.raw_content,fields,risk,automationLabel(x.workflow_source),'写回已确认字段并重新生成行动排序',actions,x.status==='PENDING'?'amber':'green')}
+function confirmAnomalyCard(x){const evidence=(x.evidence||safeJson(x.evidence_json,[])).join('；');const actions=['ANOMALY_CANDIDATE','PENDING_CONFIRMATION'].includes(x.status)?`<button class="btn primary" data-anomaly-confirm="${esc(x.candidate_id)}">确认</button><button class="btn ghost" data-anomaly-reject="${esc(x.candidate_id)}">驳回</button><button class="btn link" data-go="orders/${esc(x.order_id)}">查看订单</button>`:`<button class="btn link" data-go="orders/${esc(x.order_id)}">查看订单</button>`;return confirmCardShell('异常确认','异常确认',x.order_no||x.candidate_id,x.recommended_action,evidence,anomalyLabels[x.anomaly_type]||x.anomaly_type,x.severity,'Agent巡检','保存异常决策，不自动执行对外动作',actions,'red')}
+function confirmApprovalCard(x){const category=/MESSAGE|SEND|DRAFT/i.test(x.action_type||'')?'对外动作':'任务草稿';const orderAction=x.order_id?`<button class="btn link" data-go="orders/${esc(x.order_id)}">查看订单</button>`:'';const actions=x.status==='PENDING'?`<button class="btn primary" data-approval-approve="${esc(x.approval_id)}">批准</button><button class="btn ghost" data-approval-reject="${esc(x.approval_id)}">驳回</button>${orderAction}`:orderAction;return confirmCardShell(category,category==='对外动作'?'对外动作审批':'任务草稿确认',x.order_no||x.approval_id,x.action_type,(x.payload||safeJson(x.payload_json,{})).reason||x.approval_id,x.action_type,x.required_role==='manager'?'高 · 需要主管':'需要人工确认','Agent助手',category==='对外动作'?'只生成或放行待发送草稿，网站不会自动发送':'任务进入任务中心等待执行',actions,category==='对外动作'?'red':'blue')}
+async function confirmReviewDirect(id){if(!confirm('确认后将写回正式订单并重新排序，确定继续吗？'))return;try{const x=await api(`/api/reviews/${encodeURIComponent(id)}`);await api(`/api/reviews/${encodeURIComponent(id)}/confirm`,{method:'POST',body:JSON.stringify({candidate:x.candidate||{},operator_id:currentUser()}),timeoutMs:240000});cache={operators:cache.operators};toast('候选已确认并写回','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
+async function rejectReviewDirect(id){if(!confirm('确定驳回这条候选吗？'))return;try{await api(`/api/reviews/${encodeURIComponent(id)}/reject`,{method:'POST',body:JSON.stringify({operator_id:currentUser()})});toast('候选已驳回','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
+async function openReviewEditor(id){try{const x=await api(`/api/reviews/${encodeURIComponent(id)}`),c=x.candidate||{};openModal({eyebrow:'HUMAN REVIEW',title:x.order_no||'消息变化候选',subtitle:'确认前可编辑字段值；确认后才写回订单。',body:`<div class="quote">${esc(x.raw_content||'无原文')}</div><div class="candidate-section" id="modalCandidateFields">${(c.fields||[]).map((f,i)=>`<label class="field"><span>${esc(fieldLabel(f.field_name))}</span><input data-index="${i}" value="${esc(f.normalized_value??'')}"></label>`).join('')||'<p>没有字段候选</p>'}</div>`,actions:x.status==='PENDING'?`<button class="btn" data-close-modal>取消</button><button class="btn primary" id="modalConfirmReview">确认并写回</button>`:'<button class="btn primary" data-close-modal>关闭</button>'});$('#modalConfirmReview')?.addEventListener('click',async()=>{const edited=JSON.parse(JSON.stringify(c));$$('#modalCandidateFields input').forEach(inp=>{const f=edited.fields[Number(inp.dataset.index)];f.normalized_value=['current_progress','current_progress_percentage'].includes(f.field_name)?Number(inp.value):inp.value});try{await api(`/api/reviews/${encodeURIComponent(id)}/confirm`,{method:'POST',body:JSON.stringify({candidate:edited,operator_id:currentUser()}),timeoutMs:240000});closeModal();cache={operators:cache.operators};toast('候选已确认并写回','success');renderRoute(false)}catch(e){toast(e.message,'error')}})}catch(e){toast(e.message,'error')}}
+async function decideApproval(id,decision){if(!confirm(decision==='APPROVE'?'确定批准该动作吗？':'确定驳回该动作吗？'))return;try{await api(`/api/agent/approvals/${encodeURIComponent(id)}/decision`,{method:'POST',body:JSON.stringify({decision,operator_id:currentUser(),current_role:currentUser()==='MANAGER-1'?'manager':'operator'})});toast(decision==='APPROVE'?'审批已通过':'审批已驳回','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
+
+
 function taskRecommendedDraft(t){const text=`${t.title||''} ${t.recommended_action||''}`;if(t.target==='factory'||t.waiting_on==='factory'||/工厂|供应商|生产|物料|进度/.test(text))return 'SUPPLIER_PROGRESS_FOLLOWUP';if(/交期|交货|延期|按时/.test(text))return 'DELIVERY_STATUS_REPLY';if(/确认|版本|包装|样品|唛头|设计/.test(text))return 'CUSTOMER_CONFIRMATION_REMINDER';return 'CUSTOMER_REPLY'}
 function taskActions(t,compact=false){
   const buttons=[];const order=t.order||{};
@@ -192,50 +228,41 @@
   $$('[data-task-replied]',root).forEach(b=>b.onclick=async e=>{e.stopPropagation();await markTaskReplied(b.dataset.taskReplied)});
 }
 async function pageToday(root){
-  const d=await dashboardData();const a=d.activation||{total_orders:0,needs_initialization:0,action_generated:0,activation_target:0};const items=(d.top_actions||[]).filter(matchTask);const hasTasks=d.summary.total>0;const hasOrders=a.total_orders>0;updateBadges(d.summary.do_now+d.summary.do_today+d.summary.escalate,null);
-  const title=hasTasks?`今天有 ${d.summary.do_now+d.summary.do_today+d.summary.escalate} 项需要你决定或行动`:hasOrders&&a.needs_initialization?`已建立 ${a.total_orders} 笔订单底座，下一步补充动态进展`:'先建立订单事实，再让系统安排下一步';
-  const desc=hasTasks?'这里仅展示优先级最高的5项；全部任务用于完整查询、筛选和分派。':hasOrders&&a.needs_initialization?'原始客户订单无法直接告诉系统当前进度、最近沟通和等待状态。先完成一笔订单，再补两笔，即可看到首次跨订单行动排序。':'当前没有业务数据。先导入原始订单，再补充最新进展或沟通。';
-  const heroActions=hasOrders&&a.needs_initialization?`<button class="btn primary" data-route="activation">开始补充进展</button><button class="btn soft" data-route="orders">查看订单底座</button>`:`<button class="btn primary" data-route="intake">接入新消息</button><button class="btn soft" data-route="orders">查看订单中心</button>`;
-  const activationBanner=hasTasks&&a.needs_initialization?`<section class="activation-banner"><div><span>还有 ${a.needs_initialization} 笔订单只有基础信息</span><strong>它们不会参与正式行动排序，补充当前进展后再进入队列。</strong></div><button class="btn primary" data-route="activation">继续初始化</button></section>`:'';
-  let body='';
-  if(hasTasks){body=`${activationBanner}<section class="metric-grid">${metric('立即处理',d.summary.do_now,'red','alert','已到期或高优先级','tasks?group=urgent')}${metric('今天处理',d.summary.do_today,'amber','today','今日截止','tasks?state=DO_TODAY')}${metric('等待外部',d.summary.waiting,'blue','clock','承诺窗口内不重复催','tasks?state=WAITING_EXTERNAL')}${metric('需确认/升级',d.summary.needs_confirmation+d.summary.escalate,'green','review','需要人工决策','tasks?group=review')}</section><section class="panel"><div class="panel-head"><div><h2>优先行动 Top 5</h2><p>今日行动只保留最值得现在处理的事项，避免与全部任务重复。</p></div><div class="button-row"><button class="btn" data-route="tasks">查看全部任务</button><button class="btn primary" data-route="intake">录入消息</button></div></div><div class="action-list" id="todayList">${items.map(actionRow).join('')||emptyState('当前没有优先行动','可进入全部任务查看计划事项，或录入新消息。')}</div></section>`}
-  else if(hasOrders&&a.needs_initialization){body=`<section class="panel">${emptyState('订单已导入，但还不能判断今天先做什么','原始订单只建立客户、产品、数量和正式交期等事实。请选择活跃订单补充当前节点、联系状态或最近沟通。',`<button class="btn primary" data-route="activation">生成首次行动</button><button class="btn" data-route="orders">查看订单底座</button>`)}</section>`}
-  else if(hasOrders){body=`<section class="panel">${emptyState('当前没有开放行动','订单已完成初始化，但暂时没有开放任务。收到新消息或创建任务后，系统会重新排序。',`<button class="btn primary" data-route="intake">接入新消息</button><button class="btn" data-route="orders">查看订单</button>`)}</section>`}
-  else{body=`<section class="panel">${emptyState('还没有订单','先导入原始客户订单建立事实底座。导入后，系统会引导你补充动态进展。',`<a class="btn primary" href="/import-orders">批量导入</a><button class="btn" id="newOrderFromEmpty">新建订单</button>`)}</section>`}
-  root.innerHTML=`<div class="page-stack"><section class="hero"><div class="hero-copy"><div class="hero-eyebrow"><i></i>${esc(new Intl.DateTimeFormat('zh-CN',{year:'numeric',month:'long',day:'numeric',weekday:'long'}).format(new Date()))}</div><h2>${title}</h2><p>${desc}</p><div class="hero-actions">${heroActions}</div></div><div class="hero-side"><div class="hero-signal"><span>${currentUser()==='MANAGER-1'?'团队订单':'我的订单'}</span><strong>${a.total_orders||0}</strong></div><div class="hero-signal"><span>待补充进展</span><strong>${a.needs_initialization||0}</strong></div><div class="hero-signal"><span>已生成行动</span><strong>${a.action_generated||0}</strong></div></div></section>${body}</div>`;
-  bindRouteButtons(root);$('#newOrderFromEmpty',root)?.addEventListener('click',openNewOrderModal);bindTaskActions(root,items)
+  const role=currentUser()==='MANAGER-1'?'manager':'operator';const [d,reviews,agent]=await Promise.all([dashboardData(),api('/api/reviews?status=PENDING').catch(()=>({items:[],pending:0})),api(`/api/agent/overview?current_user_id=${encodeURIComponent(currentUser())}&current_role=${role}`).catch(()=>({reports:[],candidates:[],approvals:[],summary:{}}))]);const a=d.activation||{total_orders:0,needs_initialization:0,action_generated:0};
+  if(!a.total_orders){root.innerHTML=`<div class="page-stack">${firstLoginPanel()}</div>`;$('#firstNewOrder',root)?.addEventListener('click',openNewOrderModal);updateBadges(0,reviews.pending||0);return}
+  const all=(d.items||[]).filter(matchTask);const execution=(d.top_actions||[]).filter(t=>!t.pending_confirmation&&['DO_NOW','DO_TODAY','SCHEDULED','ESCALATE'].includes(t.action_state)).slice(0,5);const waiting=all.filter(t=>t.action_state==='WAITING_EXTERNAL').sort((x,y)=>String(x.promised_reply_at||'9999').localeCompare(String(y.promised_reply_at||'9999'))).slice(0,3);const pendingReviews=(reviews.items||[]).filter(x=>x.status==='PENDING').slice(0,3);const latest=agent.reports?.[0]?.report||null;const pendingCount=Number(reviews.pending||0)+Number(agent.summary?.pending_approval_count||0)+Number(agent.summary?.candidate_count||0);updateBadges(execution.length,pendingCount);
+  root.innerHTML=`<div class="page-stack"><div class="metric-grid">${uiMetric('优先行动',execution.length,'red','today')}${uiMetric('今日到期',all.filter(x=>x.action_state==='DO_TODAY').length,'amber','clock')}${uiMetric('超时等待',all.filter(x=>x.action_state==='WAITING_EXTERNAL'&&x.promised_reply_at&&new Date(x.promised_reply_at)<=new Date()).length,'blue','clock')}${uiMetric('待我确认',pendingCount,'amber','review')}</div><div class="quick-entries"><div class="quick-entry" id="todayNewTask"><span class="metric-icon">${icon('plus')}</span>新建任务</div><div class="quick-entry" data-go="intake"><span class="metric-icon">${icon('upload')}</span>录入消息</div><div class="quick-entry" data-go="agent"><span class="metric-icon">${icon('spark')}</span>新会话</div></div>${a.needs_initialization?`<div class="business-note">还有 ${a.needs_initialization} 笔订单只有基础信息，补充当前进度或最近沟通后才会进入正式行动排序。 <button class="btn link" data-go="activation">继续初始化</button></div>`:''}<h4 class="section-title">当前行动</h4><div class="proto-2col"><section class="panel"><div class="panel-head"><h3>当前执行队列</h3><span class="tag muted">仅含已确认的正式任务</span></div><div class="panel-body">${execution.map(t=>uiActionRow(t,'处理')).join('')||emptyState('当前没有优先行动','已确认的正式任务会按优先级出现在这里。')}</div></section><section class="panel"><div class="panel-head"><h3>等待即将到期</h3><span class="tag warning">${waiting.length} 项</span></div><div class="panel-body">${waiting.map(t=>uiActionRow(t,'查看')).join('')||emptyState('暂无临近等待','承诺回复时间临近或超时后会进入这里。')}</div></section></div><h4 class="section-title">监控与待确认</h4><div class="proto-2col"><section class="panel"><div class="panel-head"><h3>最近一次 Agent 运行</h3><span class="tag ${latest?'success':'muted'}">${latest?'已完成':'暂无运行'}</span></div><div class="panel-body"><div class="agent-run-summary"><div class="summary-cell"><small>扫描订单</small><strong>${Number(latest?.screened_order_count||0)}</strong></div><div class="summary-cell"><small>风险订单</small><strong>${Number(latest?.risk_order_count||0)}</strong></div><div class="summary-cell"><small>信息缺口</small><strong>${Number(latest?.information_gap_order_count||0)}</strong></div></div><div class="run-row"><span class="r-label">运行时间</span><span class="r-val">${esc(latest?.generated_at?fdt(latest.generated_at):'尚未运行')}</span></div><div class="row-actions" style="margin-top:10px"><button class="btn link" data-go="agent">查看运行结果</button></div></div></section><section class="panel"><div class="panel-head"><h3>待本人确认</h3><span class="tag danger">${pendingCount} 项</span></div><div class="panel-body">${pendingReviews.map(todayReviewRow).join('')||'<p class="demo-note">消息字段变更、异常确认和Agent审批会统一进入确认中心。</p>'}<div class="row-actions" style="margin-top:10px"><button class="btn primary" data-go="confirm">去确认中心</button></div></div></section></div></div>`;
+  $('#todayNewTask',root).onclick=()=>openTaskModal();bindTaskActions(root,[...execution,...waiting]);bindRouteButtons(root)
 }
 
 async function pageTasks(root){
-  const d=await dashboardData();let items=d.items.filter(matchTask);const initialState=route.query.state||'';const initialGroup=route.query.group||'';if(initialState)activeTaskFilter=initialState;else if(initialGroup==='urgent')activeTaskFilter='URGENT_GROUP';else if(initialGroup==='review')activeTaskFilter='REVIEW_GROUP';
-  const scope=currentUser()==='MANAGER-1'?'团队全部任务':'只显示分配给我的任务';
-  root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>${currentUser()==='MANAGER-1'?'团队任务操作台':'我的任务操作台'}</h2><p>${scope}；今日行动只展示Top 5，本页用于完整筛选、分派和处理。</p></div><div class="button-row"><button class="btn" id="refreshRanking">刷新排序</button><button class="btn primary" id="newTaskButton">新建任务</button></div></div><section class="metric-grid">${metric('全部开放任务',items.filter(x=>x.status!=='DONE').length,'green','tasks','查看全部','tasks?state=ALL')}${metric('立即/升级',items.filter(x=>['DO_NOW','ESCALATE'].includes(x.action_state)).length,'red','alert','需要优先处理','tasks?group=urgent')}${metric('等待外部',items.filter(x=>x.action_state==='WAITING_EXTERNAL').length,'blue','clock','等待承诺回复','tasks?state=WAITING_EXTERNAL')}${metric('待确认',items.filter(x=>x.action_state==='NEEDS_CONFIRMATION').length,'amber','review','需要人工判断','tasks?state=NEEDS_CONFIRMATION')}</section><section class="panel"><div class="filter-row"><div class="tabs" id="taskTabs">${[['ALL','全部'],['URGENT_GROUP','立即/升级'],['DO_TODAY','今天处理'],['WAITING_EXTERNAL','等待外部'],['NEEDS_CONFIRMATION','待确认'],['SCHEDULED','已计划'],['DONE','已完成']].map(([k,l])=>`<button class="tab ${activeTaskFilter===k?'active':''}" data-task-filter="${k}">${esc(l)}</button>`).join('')}</div>${currentUser()==='MANAGER-1'?`<select class="filter-select" id="ownerFilter"><option value="ALL">全部负责人</option>${(cache.operators||[]).filter(o=>o.user_id!=='MANAGER-1').map(o=>`<option value="${esc(o.user_id)}">${esc(o.name)}</option>`).join('')}</select>`:`<span class="tag">当前身份：${esc(operatorNames[currentUser()]||currentUser())}</span>`}</div><div class="data-table-wrap"><table class="data-table"><thead><tr><th>任务</th><th>订单</th><th>状态</th><th>负责人</th><th>下一时间</th><th>风险</th><th>操作</th></tr></thead><tbody id="taskTable"></tbody></table></div></section></div>`;
-  const render=()=>{const owner=$('#ownerFilter',root)?.value||'ALL';const rows=items.filter(t=>{const stateOk=activeTaskFilter==='ALL'||(activeTaskFilter==='URGENT_GROUP'?['DO_NOW','ESCALATE'].includes(t.action_state):activeTaskFilter==='REVIEW_GROUP'?['NEEDS_CONFIRMATION','ESCALATE'].includes(t.action_state):t.action_state===activeTaskFilter);return stateOk&&(owner==='ALL'||t.owner_user_id===owner)});$('#taskTable',root).innerHTML=rows.map(t=>`<tr class="clickable" data-task-row="${esc(t.task_id)}"><td><div class="cell-title"><strong>${esc(t.title)}</strong><small>${esc(t.recommended_action||'')}</small></div></td><td><div class="cell-title"><strong>${esc(t.order?.order_no||'—')}</strong><small>${esc(t.order?.customer_name||'')}</small></div></td><td>${statusBadge(t.action_state)}</td><td>${esc(operatorNames[t.owner_user_id]||t.owner_user_id||'未分配')}</td><td>${esc(fdt(t.next_action_at||t.business_deadline))}<br><small>${esc(relative(t.next_action_at||t.business_deadline))}</small></td><td>${riskBadge(t.risk_level||'none')}</td><td><div class="row-actions">${taskActions(t,true)}<button class="btn small" data-task-detail="${esc(t.task_id)}">详情</button></div></td></tr>`).join('')||`<tr><td colspan="7">${emptyState('没有匹配任务','调整筛选条件或创建一条新任务。')}</td></tr>`;$$('[data-task-row]',root).forEach(r=>r.onclick=()=>openTaskDrawer(items.find(t=>t.task_id===r.dataset.taskRow)));bindTaskActions(root,rows);bindRouteButtons(root)};
-  render();$$('[data-task-filter]',root).forEach(b=>b.onclick=()=>{activeTaskFilter=b.dataset.taskFilter;$$('[data-task-filter]',root).forEach(x=>x.classList.toggle('active',x===b));render()});if($('#ownerFilter',root))$('#ownerFilter',root).onchange=render;$('#newTaskButton',root).onclick=()=>openTaskModal();$('#refreshRanking',root).onclick=async()=>{try{await api('/api/coze/ft04/refresh',{method:'POST',body:JSON.stringify({current_user_id:currentUser()})});cache.dashboard=null;toast('行动排序已刷新','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
+  const d=await dashboardData();let items=(d.items||[]).filter(matchTask);const filter=route.query.state||activeTaskFilter||'ALL';activeTaskFilter=filter;const filtered=items.filter(t=>filter==='ALL'||t.action_state===filter||(filter==='URGENT_GROUP'&&['DO_NOW','ESCALATE'].includes(t.action_state)));
+  root.innerHTML=`<div class="page-stack"><div class="proto-3col">${uiMetric('进行中',items.filter(x=>x.status!=='DONE').length,'blue','tasks')}${uiMetric('等待外部',items.filter(x=>x.action_state==='WAITING_EXTERNAL').length,'amber','clock')}${uiMetric('本周完成',items.filter(x=>x.status==='DONE').length,'green','check')}</div><section class="panel"><div class="panel-head"><h3>全部任务</h3><div class="row-actions"><select class="btn secondary" id="taskFilter"><option value="ALL">全部状态</option><option value="URGENT_GROUP" ${filter==='URGENT_GROUP'?'selected':''}>立即/升级</option><option value="DO_TODAY" ${filter==='DO_TODAY'?'selected':''}>今天处理</option><option value="WAITING_EXTERNAL" ${filter==='WAITING_EXTERNAL'?'selected':''}>等待外部</option><option value="NEEDS_CONFIRMATION" ${filter==='NEEDS_CONFIRMATION'?'selected':''}>待确认</option><option value="DONE" ${filter==='DONE'?'selected':''}>已完成</option></select><button class="btn secondary" id="refreshRanking">刷新排序</button><button class="btn primary" id="newTaskButton">新建任务</button></div></div><div class="panel-body" style="padding:0"><table class="data-table"><thead><tr><th>任务</th><th>来源</th><th>负责人</th><th>截止</th><th>等待对象</th><th>状态</th><th></th></tr></thead><tbody>${filtered.map(taskTableRow).join('')||'<tr><td colspan="7">没有匹配任务</td></tr>'}</tbody></table></div></section></div>`;
+  $('#taskFilter',root).onchange=e=>{activeTaskFilter=e.target.value;go(`tasks?state=${encodeURIComponent(e.target.value)}`)};$('#newTaskButton',root).onclick=()=>openTaskModal();$('#refreshRanking',root).onclick=async()=>{try{await api('/api/coze/ft04/refresh',{method:'POST',body:JSON.stringify({current_user_id:currentUser()})});cache.dashboard=null;toast('行动排序已刷新','success');renderRoute(false)}catch(e){toast(e.message,'error')}};bindTaskActions(root,filtered);
 }
 
 async function pageActivation(root){
-  const [a,d]=await Promise.all([api(`/api/activation/summary?current_user_id=${encodeURIComponent(currentUser())}`),ordersData()]);const target=a.activation_target||0;const generated=a.action_generated||0;const remaining=Math.max(0,target-generated);const phase=generated===0?1:remaining>0?2:3;const recommended=(a.recommended_orders||[]).filter(matchOrder);
-  const phaseText=phase===1?'先完成1笔：快速获得第一条行动':phase===2?`第一条行动已生成，再补 ${remaining} 笔展示跨订单排序`:'初始化目标已完成，可以查看首次排序';
-  const cards=recommended.map((o,i)=>`<article class="activation-order"><div class="activation-order-head"><div><span>${i===0&&phase===1?'建议从这笔开始':'推荐活跃订单'}</span><h3>${esc(o.order_no)} · ${esc(o.customer_name||'未知客户')}</h3><p>${esc(o.product_name||'未填写产品')} · 客户交期 ${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</p></div>${readinessBadge(o.action_readiness)}</div><div class="activation-order-actions"><button class="btn primary" data-quick-init="${esc(o.order_id)}">快速补充状态</button><button class="btn" data-intake-order="${esc(o.order_id)}">粘贴最近沟通</button><button class="btn ghost" data-go="orders/${esc(o.order_id)}">查看订单</button></div></article>`).join('');
-  root.innerHTML=`<div class="page-stack"><section class="activation-hero"><div><span>FIRST VALUE LOOP</span><h2>${phaseText}</h2><p>一笔订单负责证明系统能给出下一步；三笔订单负责证明系统能比较今天先做哪一笔。无需一次完善全部订单。</p></div><div class="activation-count"><strong>${Math.min(generated,target)}/${target||0}</strong><span>已生成行动</span></div></section><section class="activation-steps"><div class="done"><b>1</b><span>导入原始订单<small>建立事实底座</small></span></div><div class="${generated>0?'done':'active'}"><b>2</b><span>先补1笔<small>获得首条行动</small></span></div><div class="${generated>=target&&target?'done':generated>0?'active':''}"><b>3</b><span>再补2笔<small>形成比较样本</small></span></div><div class="${a.ranking_experience_ready?'active':''}"><b>4</b><span>查看排序<small>理解优先级原因</small></span></div></section><section class="metric-grid">${metric('订单底座',a.total_orders,'green','orders','已导入')}${metric('待补充进展',a.needs_initialization,'amber','edit','不参与正式排序')}${metric('已生成行动',a.action_generated,'blue','tasks','可进入排序')}${metric('首次体验目标',target,'red','chart','最多3笔')}</section>${a.ranking_experience_ready?`<section class="activation-success"><div><span>首次排序样本已准备好</span><strong>现在可以比较立即处理、等待回复和暂缓处理的差异。</strong></div><button class="btn primary" data-route="today">查看首次行动排序</button></section>`:''}<section class="panel"><div class="panel-head"><div><h2>${recommended.length?'选择活跃订单补充动态信息':'当前没有待初始化订单'}</h2><p>系统仅按客户交期推荐初始化对象，这不是正式风险排序。正式排序会结合节点、联系状态、等待窗口和异常。</p></div><a class="btn" href="/import-orders">继续导入</a></div><div class="activation-order-list">${cards||emptyState(a.total_orders?'订单均已初始化或关闭':'还没有订单','导入原始订单后，从这里完成首次行动初始化。',a.total_orders?'':'<a class="btn primary" href="/import-orders">批量导入</a>')}</div></section></div>`;
-  bindRouteButtons(root);$$('[data-quick-init]',root).forEach(b=>b.onclick=()=>{const o=d.items.find(x=>x.order_id===b.dataset.quickInit);openQuickInitializeModal(o,()=>{cache={operators:cache.operators};renderRoute(false)})});$$('[data-intake-order]',root).forEach(b=>b.onclick=()=>go(`intake?order=${encodeURIComponent(b.dataset.intakeOrder)}`));$$('[data-go]',root).forEach(b=>b.onclick=()=>go(b.dataset.go));
+  const [a,d]=await Promise.all([api(`/api/activation/summary?current_user_id=${encodeURIComponent(currentUser())}`),ordersData()]);const recommended=(a.recommended_orders||[]).filter(matchOrder);
+  root.innerHTML=`<div class="page-stack"><div class="metric-grid">${uiMetric('订单底座',a.total_orders,'blue','orders')}${uiMetric('待补充进展',a.needs_initialization,'amber','edit')}${uiMetric('已生成行动',a.action_generated,'green','tasks')}${uiMetric('首次体验目标',a.activation_target,'red','chart')}</div><section class="panel"><div class="panel-head"><h3>完成首次行动初始化</h3><a class="btn secondary" href="/import-orders">继续导入</a></div><div class="panel-body"><div class="flow"><span class="step">① 导入原始订单</span><span class="arrow">→</span><span class="step">② 补充1笔动态</span><span class="arrow">→</span><span class="step">③ 再补2笔形成排序</span></div>${recommended.map(o=>`<div class="action-row" style="display:flex;gap:14px;align-items:center;padding:14px 0;border-bottom:1px solid var(--line)"><div style="flex:1"><strong>${esc(o.order_no)} · ${esc(o.customer_name||'未知客户')}</strong><br><small class="demo-note">${esc(o.product_name||'未填写产品')} · 客户交期 ${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</small></div><button class="btn primary" data-quick-init="${esc(o.order_id)}">快速补充状态</button><button class="btn secondary" data-intake-order="${esc(o.order_id)}">粘贴最近沟通</button></div>`).join('')||emptyState('没有待初始化订单','已完成初始化的订单会进入今日工作台和任务中心。')}</div></section></div>`;
+  $$('[data-quick-init]',root).forEach(b=>b.onclick=()=>{const o=d.items.find(x=>x.order_id===b.dataset.quickInit);openQuickInitializeModal(o,()=>{cache={operators:cache.operators};renderRoute(false)})});$$('[data-intake-order]',root).forEach(b=>b.onclick=()=>go(`intake?order=${encodeURIComponent(b.dataset.intakeOrder)}`));
 }
 
 function orderPriorityScore(o){const risk={none:0,low:1,medium:2,high:3,critical:4}[o.max_risk||'none']||0;const readiness={ACTION_GENERATED:4,READY_FOR_RANKING:3,NEEDS_STATUS:2,BASE_ONLY:1,CLOSED:0}[o.action_readiness||'BASE_ONLY']||0;return risk*10000+Number(o.open_task_count||0)*1000+readiness*100}
 function orderSortReason(o){if(['critical','high'].includes(o.max_risk))return `${riskLabels[o.max_risk]}，优先查看`;if(Number(o.open_task_count||0)>0)return `${o.open_task_count}项开放任务`;if(readinessNeedsInput(o.action_readiness))return '待补充进展';const date=o.requested_delivery_date||o.customer_delivery_date;return date?`客户交期 ${fdate(date)}`:'按最近更新时间'}
 function sortOrders(items,mode){const rows=[...items];if(mode==='DELIVERY')return rows.sort((a,b)=>String(a.requested_delivery_date||a.customer_delivery_date||'9999').localeCompare(String(b.requested_delivery_date||b.customer_delivery_date||'9999')));if(mode==='UPDATED')return rows.sort((a,b)=>String(b.updated_at||'').localeCompare(String(a.updated_at||'')));if(mode==='ORDER_NO')return rows.sort((a,b)=>String(a.order_no||'').localeCompare(String(b.order_no||''),'zh-CN'));return rows.sort((a,b)=>orderPriorityScore(b)-orderPriorityScore(a)||String(a.next_action_at||a.requested_delivery_date||'9999').localeCompare(String(b.next_action_at||b.requested_delivery_date||'9999')))}
 async function pageOrders(root){
-  const d=await ordersData();const raw=d.items.filter(matchOrder);const items=sortOrders(raw,activeOrderSort);const activationNote=d.summary.base_only?`<section class="activation-banner"><div><span>${d.summary.base_only} 笔订单仍只有基础信息</span><strong>原始订单不会自动产生风险结论。先补充进展，再进入今日行动排序。</strong></div><button class="btn primary" data-route="activation">开始初始化</button></section>`:'';const scope=currentUser()==='MANAGER-1'?'团队全部订单':'只显示负责人为我的订单';
-  root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>${currentUser()==='MANAGER-1'?'团队订单中心':'我的订单中心'}</h2><p>${scope}。默认按“行动优先”排列，并在每张订单卡中说明排序依据。</p></div><div class="button-row"><label class="sort-control"><span>排列方式</span><select id="orderSort"><option value="ACTION" ${activeOrderSort==='ACTION'?'selected':''}>行动优先</option><option value="DELIVERY" ${activeOrderSort==='DELIVERY'?'selected':''}>客户交期从近到远</option><option value="UPDATED" ${activeOrderSort==='UPDATED'?'selected':''}>最近更新</option><option value="ORDER_NO" ${activeOrderSort==='ORDER_NO'?'selected':''}>订单号</option></select></label><a class="btn" href="/import-orders">${icon('upload')}批量导入</a><button class="btn primary" id="newOrder">${icon('plus')}新建订单</button></div></div>${activationNote}<section class="metric-grid">${metric('有效订单',d.summary.active,'green','orders','当前可见订单','orders')}${metric('待补充进展',d.summary.base_only,'amber','edit','只有基础订单','activation')}${metric('开放任务',d.summary.pending_tasks,'red','tasks','进入任务明细','tasks')}${metric('已有行动订单',d.summary.action_generated,'blue','chart','已经进入排序','tasks')}</section>${items.length?`<div class="sort-explanation"><strong>当前排序：${activeOrderSort==='ACTION'?'行动优先':activeOrderSort==='DELIVERY'?'客户交期从近到远':activeOrderSort==='UPDATED'?'最近更新':'订单号'}</strong><span>${activeOrderSort==='ACTION'?'先看严重风险和开放任务，再看待初始化订单与交期。':'可随时切回“行动优先”。'}</span></div><section class="order-grid">${items.map((o,i)=>orderCard(o,i+1)).join('')}</section>`:`<section class="panel">${emptyState('没有匹配订单','请确认表格负责人是否与当前身份一致，或清除搜索条件。',`<button class="btn primary" id="emptyNewOrder">新建订单</button><a class="btn" href="/import-orders">批量导入</a>`)}</section>`}</div>`;
-  bindRouteButtons(root);$('#newOrder',root).onclick=openNewOrderModal;$('#emptyNewOrder',root)?.addEventListener('click',openNewOrderModal);$('#orderSort',root).onchange=e=>{activeOrderSort=e.target.value;renderRoute(false)};$$('[data-order-card]',root).forEach(c=>c.onclick=()=>go(`orders/${c.dataset.orderCard}`))
+  const d=await ordersData();const raw=d.items.filter(matchOrder);const items=sortOrders(raw,activeOrderSort);const riskCount=raw.filter(o=>!['none',null,undefined].includes(o.max_risk)).length;const waitingCount=raw.filter(o=>o.contact_status==='WAITING_REPLY'||o.waiting_on).length;
+  if(!raw.length){root.innerHTML=`<div class="page-stack">${firstLoginPanel()}</div>`;$('#firstNewOrder',root)?.addEventListener('click',openNewOrderModal);return}
+  root.innerHTML=`<div class="page-stack"><div class="proto-3col">${uiMetric('进行中订单',d.summary.active,'blue','orders')}${uiMetric('风险订单',riskCount,'red','alert')}${uiMetric('等待中订单',waitingCount,'amber','clock')}</div><div class="row-actions" style="margin:2px 0 4px"><a class="btn primary" href="/import-orders">导入订单底座</a><button class="btn secondary" id="newOrder">新建订单</button><select class="btn secondary" id="orderSort"><option value="ACTION" ${activeOrderSort==='ACTION'?'selected':''}>按行动优先</option><option value="DELIVERY" ${activeOrderSort==='DELIVERY'?'selected':''}>按客户交期</option><option value="UPDATED" ${activeOrderSort==='UPDATED'?'selected':''}>按最近更新</option><option value="ORDER_NO" ${activeOrderSort==='ORDER_NO'?'selected':''}>按订单号</option></select></div><section class="panel"><div class="panel-head"><h3>订单列表</h3><span class="tag muted">${items.length} 笔 · 当前可见</span></div><div class="panel-body" style="padding:0"><table class="data-table"><thead><tr><th>订单 / 客户</th><th>当前节点</th><th>负责人</th><th>客户正式交期</th><th>当前风险</th><th>下一步</th><th>开放任务</th><th></th></tr></thead><tbody>${items.map(orderTableRow).join('')}</tbody></table></div></section></div>`;
+  $('#newOrder',root).onclick=openNewOrderModal;$('#orderSort',root).onchange=e=>{activeOrderSort=e.target.value;renderRoute(false)};$$('[data-order-detail]',root).forEach(b=>b.onclick=()=>go(`orders/${b.dataset.orderDetail}`));
 }
 function orderCard(o,rank){const p=progressValue(o.current_progress),readiness=o.action_readiness||'BASE_ONLY';return `<article class="order-card" data-order-card="${esc(o.order_id)}" tabindex="0"><div class="order-rank"><b>${rank}</b><span>${esc(orderSortReason(o))}</span></div><div class="order-card-head"><div><h3>${esc(o.order_no)}</h3><p>${esc(o.customer_name||'未知客户')} · ${esc(o.product_name||'未填写产品')}</p></div><div class="badge-stack">${readinessBadge(readiness)}${riskBadge(o.max_risk||'none')}</div></div><div class="order-meta"><div class="meta-item"><span>当前节点</span><strong>${esc(o.current_node||'待补充')}</strong></div><div class="meta-item"><span>客户交期</span><strong>${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</strong></div><div class="meta-item"><span>开放任务</span><strong>${Number(o.open_task_count||0)}</strong></div><div class="meta-item"><span>负责人</span><strong>${esc(operatorNames[o.owner]||o.owner||'未分配')}</strong></div></div><div><div class="order-card-foot"><small>订单进度</small><strong>${o.current_progress==null?'待补充':`${Math.round(p)}%`}</strong></div><div class="progress"><i style="width:${p}%"></i></div></div><div class="order-card-foot"><small>下一步：${esc(readinessNextAction(o))}</small><span>${icon('arrow')}</span></div></article>`}
 
 async function pageOrderDetail(root){
-  const d=await api(`/api/orders/${encodeURIComponent(route.id)}?current_user_id=${encodeURIComponent(currentUser())}`);const o=d.order;const p=progressValue(o.current_progress);const openTasks=d.tasks.filter(t=>t.status!=='DONE');const readiness=o.action_readiness||'BASE_ONLY';const needsInput=readinessNeedsInput(readiness);const activationPanel=needsInput?`<section class="activation-banner prominent"><div><span>这笔订单目前只有基础事实</span><strong>补充最近沟通或快速状态后，系统才会生成可靠行动。原始订单本身不会被包装成风险结论。</strong></div><div class="button-row"><button class="btn primary" id="quickInitialize">快速补充状态</button><button class="btn" id="activationIntake">粘贴最近沟通</button></div></section>`:'';
-  root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div class="button-row"><button class="btn ghost" data-route="orders">← 返回订单中心</button></div><div class="button-row"><button class="btn" id="editOrder">${icon('edit')}编辑订单</button><button class="btn" id="orderIntake">${icon('message')}录入消息</button>${needsInput?'':`<button class="btn bronze" id="orderCommunicate">${icon('spark')}生成沟通</button>`}<button class="btn primary" id="orderNewTask">${icon('plus')}新增任务</button></div></div>${activationPanel}<section class="order-hero"><div><span class="order-kicker">ORDER WORKSPACE</span><h2>${esc(o.order_no)}</h2><p>${esc(o.customer_name||'未知客户')} · ${esc(o.product_name||'未填写产品')} · ${esc(o.status||'ACTIVE')}</p><div class="hero-actions"><span class="tag">${esc(o.current_node||'待补充节点')}</span>${readinessBadge(readiness)}${riskBadge(maxRiskFromList(d.risks))}</div></div><div class="order-hero-stats"><div class="order-hero-stat"><span>客户正式交期</span><strong>${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</strong></div><div class="order-hero-stat"><span>工厂完成承诺</span><strong>${esc(fdate(o.latest_supplier_commitment))}</strong></div><div class="order-hero-stat"><span>当前进度</span><strong>${o.current_progress==null?'待补充':`${Math.round(p)}%`}</strong></div><div class="order-hero-stat"><span>开放任务</span><strong>${openTasks.length}</strong></div></div></section><section class="detail-grid"><div class="page-stack"><section class="panel"><div class="panel-head"><div><h2>订单事实</h2><p>静态订单事实与动态行动状态分开管理</p></div></div><div class="panel-body"><div class="info-grid">${orderFactCards(o)}</div></div></section><section class="panel"><div class="panel-head"><div><h2>关联任务</h2><p>${needsInput?'完成状态初始化后，系统会生成第一条行动':'沟通动作直接从任务触发，发送后进入等待状态'}</p></div><button class="btn small" id="orderNewTask2">新增任务</button></div><div class="action-list">${openTasks.length?openTasks.map(t=>actionRow({...t,action_state:deriveTaskStateForDetail(t),order:o})).join(''):emptyState(needsInput?'尚未生成行动':'暂无开放任务',needsInput?'这不是“当前无需行动”，而是动态信息不足。请先补充进展。':'为该订单创建一条真实任务，或从客户消息自动转成任务。',needsInput?'<button class="btn primary" id="quickInitializeInline">补充进展</button>':'')}</div></section><section class="panel"><div class="panel-head"><div><h2>沟通记录</h2><p>原始消息、工厂回复和客户变更都保留证据</p></div></div><div class="panel-body">${d.messages.length?`<div class="timeline">${d.messages.map(m=>timelineItem(`${m.sender_role==='factory'?'工厂':'客户'} · ${m.source_channel}`,m.raw_content,m.source_time||m.created_at)).join('')}</div>`:emptyState('还没有沟通记录','选择“粘贴最近沟通”，AI会在订单上下文中提取动态信息。')}</div></section></div><aside class="page-stack"><section class="panel"><div class="panel-head"><div><h3>行动准备状态</h3><p>决定是否可以进入排序</p></div></div><div class="panel-body"><div class="readiness-summary">${readinessBadge(readiness)}<p>${needsInput?'缺少当前节点、联系状态或最近沟通。':'动态信息已具备，可通过任务进入行动队列。'}</p></div></div></section><section class="panel"><div class="panel-head"><div><h3>当前风险</h3><p>开放风险信号</p></div></div><div class="panel-body"><div class="risk-list">${d.risks.length?d.risks.map(r=>`<div class="risk-item ${esc(r.risk_level)}"><strong>${esc(r.risk_type)}</strong><p>${esc(r.evidence||'')}</p></div>`).join(''):emptyState(needsInput?'暂无法判断风险':'暂无开放风险',needsInput?'基础订单不足以判断风险，补充动态进展后再评估。':'订单目前没有已记录的风险信号。')}</div></div></section><section class="panel"><div class="panel-head"><div><h3>承诺与变更</h3><p>确认后的时间线</p></div></div><div class="panel-body"><div class="timeline">${[...d.commitments.map(c=>({title:c.commitment_type,body:c.commitment_value,time:c.created_at})),...d.events.slice(0,10).map(e=>({title:e.event_type,body:eventSummary(e.payload_json),time:e.created_at}))].sort((a,b)=>String(b.time).localeCompare(String(a.time))).map(x=>timelineItem(x.title,x.body,x.time)).join('')||emptyState('暂无历史','确认写回或编辑订单后，这里会形成审计时间线。')}</div></div></section></aside></section></div>`;
-  bindRouteButtons(root);const tasks=openTasks.map(t=>({...t,action_state:deriveTaskStateForDetail(t),order:o}));bindTaskActions(root,tasks);$('#editOrder',root).onclick=()=>openEditOrderModal(o);$('#orderNewTask',root).onclick=$('#orderNewTask2',root).onclick=()=>openTaskModal(o);const intakeHandler=()=>go(`intake?order=${encodeURIComponent(o.order_id)}`);$('#orderIntake',root).onclick=intakeHandler;$('#activationIntake',root)?.addEventListener('click',intakeHandler);$('#orderCommunicate',root)?.addEventListener('click',()=>openCommunicationDrawer({mode:'ft06',order:o,draftType:'CUSTOMER_REPLY'}));const openInit=()=>openQuickInitializeModal(o,()=>{cache={operators:cache.operators};renderRoute(false)});$('#quickInitialize',root)?.addEventListener('click',openInit);$('#quickInitializeInline',root)?.addEventListener('click',openInit);
+  const d=await api(`/api/orders/${encodeURIComponent(route.id)}?current_user_id=${encodeURIComponent(currentUser())}`);const o=d.order;const tasks=d.tasks.filter(t=>t.status!=='DONE').map(t=>({...t,action_state:deriveTaskStateForDetail(t),order:o}));const risks=d.risks||[];const p=progressValue(o.current_progress);const risk=maxRiskFromList(risks);const needsInput=readinessNeedsInput(o.action_readiness||'BASE_ONLY');
+  root.innerHTML=`<div class="page-stack"><section class="panel"><div class="panel-head"><h3>${esc(o.order_no)} · ${esc(o.customer_name||'未知客户')}</h3><div class="row-actions">${riskBadge(risk)}<button class="btn secondary" id="editOrder">编辑订单</button><button class="btn secondary" id="orderIntake">录入消息</button><button class="btn primary" id="orderNewTask">新建任务</button></div></div><div class="panel-body"><div class="order-hero"><div class="stat"><small>产品</small><strong>${esc(o.product_name||'—')}</strong></div><div class="stat"><small>客户正式交期</small><strong>${esc(fdate(o.requested_delivery_date||o.customer_delivery_date))}</strong></div><div class="stat"><small>供应商完工承诺</small><strong>${esc(fdate(o.latest_supplier_commitment))}</strong></div><div class="stat"><small>当前进度</small><strong>${o.current_progress==null?'待补充':`${Math.round(p)}%`}</strong></div></div><div class="tabs" style="margin:18px -20px 0"><button class="tab active" data-detail-tab="overview">概览</button><button class="tab" data-detail-tab="comm">沟通与变化</button><button class="tab" data-detail-tab="tasks">任务与等待</button><button class="tab" data-detail-tab="risk">风险与历史</button></div></div><div class="panel-body"><div class="tab-panel active" data-detail-panel="overview"><div class="proto-2col"><div><p class="demo-note">核心事实</p><p style="font-size:13px;line-height:1.7">${esc(o.customer_name||'未知客户')} · ${esc(o.product_name||'未填写产品')} · ${esc(o.quantity!=null?`${o.quantity} ${o.unit||''}`:'数量待补充')}。当前节点：${esc(o.current_node||'待补充')}；负责人：${esc(operatorNames[o.owner]||o.owner||'未分配')}。</p></div><div><p class="demo-note">当前状态</p><ul class="timeline"><li><span class="dot"></span><div><strong>${esc(o.current_node||'待补充当前节点')}</strong><br><small class="demo-note">${needsInput?'动态信息不足，暂不生成风险结论':`已有 ${tasks.length} 项开放任务`}</small></div></li><li><span class="dot muted"></span><div><strong>下一步</strong><br><small class="demo-note">${esc(readinessNextAction(o))}</small></div></li></ul></div></div>${needsInput?`<div class="row-actions" style="margin-top:14px"><button class="btn primary" id="quickInitialize">快速补充状态</button></div>`:''}</div><div class="tab-panel" data-detail-panel="comm"><ul class="timeline">${(d.messages||[]).map(m=>`<li><span class="dot"></span><div><strong>${m.sender_role==='factory'?'工厂/供应商':'客户'} · ${esc(m.source_channel||'')}</strong><br><small class="demo-note">${esc(m.raw_content||'')} · ${esc(fdt(m.source_time||m.created_at))}</small></div></li>`).join('')||'<li><span class="dot muted"></span><div><strong>暂无沟通记录</strong><br><small class="demo-note">从消息中心录入真实客户或工厂消息。</small></div></li>'}</ul></div><div class="tab-panel" data-detail-panel="tasks"><table class="data-table"><thead><tr><th>任务</th><th>来源</th><th>截止</th><th>状态</th></tr></thead><tbody>${tasks.map(t=>orderTaskRow(t)).join('')||'<tr><td colspan="4">暂无开放任务</td></tr>'}</tbody></table></div><div class="tab-panel" data-detail-panel="risk">${risks.map(r=>`<div class="cc-evidence" style="border-left-color:var(--${r.risk_level==='high'||r.risk_level==='critical'?'red':r.risk_level==='medium'?'amber':'green'})"><strong>${esc(r.risk_type)}</strong><br>${esc(r.evidence||'')}</div>`).join('')||'<div class="cc-evidence"><strong>暂无开放风险</strong><br>系统未发现已记录的风险信号。</div>'}<ul class="timeline" style="margin-top:14px">${(d.events||[]).slice(0,12).map(e=>`<li><span class="dot"></span><div><strong>${esc(e.event_type)}</strong><br><small class="demo-note">${esc(eventSummary(e.payload_json))} · ${esc(fdt(e.created_at))}</small></div></li>`).join('')}</ul></div></div></section></div>`;
+  $$('[data-detail-tab]',root).forEach(tab=>tab.onclick=()=>{$$('[data-detail-tab]',root).forEach(x=>x.classList.toggle('active',x===tab));$$('[data-detail-panel]',root).forEach(x=>x.classList.toggle('active',x.dataset.detailPanel===tab.dataset.detailTab))});
+  $('#editOrder',root).onclick=()=>openEditOrderModal(o);$('#orderIntake',root).onclick=()=>go(`intake?order=${encodeURIComponent(o.order_id)}`);$('#orderNewTask',root).onclick=()=>openTaskModal(o);$('#quickInitialize',root)?.addEventListener('click',()=>openQuickInitializeModal(o,()=>{cache={operators:cache.operators};renderRoute(false)}));bindTaskActions(root,tasks);
 }
 function maxRiskFromList(list){const rank={none:0,low:1,medium:2,high:3,critical:4};return list.reduce((a,r)=>rank[r.risk_level]>rank[a]?r.risk_level:a,'none')}
 function deriveTaskStateForDetail(t){if(t.status==='DONE')return'DONE';if(t.pending_confirmation)return'NEEDS_CONFIRMATION';if(t.waiting_on)return new Date(t.promised_reply_at)>new Date()?'WAITING_EXTERNAL':'DO_NOW';if(t.urgent||t.risk_level==='critical')return'ESCALATE';const due=new Date(t.next_action_at||t.business_deadline);if(!Number.isNaN(due.getTime())){const diff=due-new Date();if(diff<=0)return'DO_NOW';if(diff<86400000)return'DO_TODAY'}return'SCHEDULED'}
@@ -244,19 +271,33 @@
 function eventSummary(v){const p=safeJson(v,{});if(typeof p==='string')return p;const keys=Object.keys(p).slice(0,4);return keys.map(k=>`${k}: ${typeof p[k]==='object'?JSON.stringify(p[k]):p[k]}`).join('；')||'记录已更新'}
 
 async function pageIntake(root){
-  const orders=(await ordersData()).items;const preselect=route.query.order||'';root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>录入真实沟通</h2><p>提交后转入后台识别；你可以继续浏览网站，完成后再进入人工确认</p></div><div class="button-row"><button class="btn" data-route="confirm">查看待确认</button></div></div><section class="intake-layout"><form class="panel" id="intakeForm"><div class="panel-head"><div><h2>消息内容</h2><p>系统会根据发送方选择对应的信息识别能力，并生成待确认候选</p></div></div><div class="panel-body page-stack"><div class="channel-grid"><div class="channel-option"><input id="ch-email" name="source_channel" value="email" type="radio" checked><label for="ch-email">${icon('mail')}邮件</label></div><div class="channel-option"><input id="ch-wechat" name="source_channel" value="wechat" type="radio"><label for="ch-wechat">${icon('message')}企业微信</label></div><div class="channel-option"><input id="ch-manual" name="source_channel" value="internal" type="radio"><label for="ch-manual">${icon('edit')}人工记录</label></div></div><div class="form-grid"><label class="field"><span>发送方</span><select name="sender_role"><option value="customer">客户</option><option value="factory">工厂/供应商</option></select></label><label class="field"><span>关联订单 <small>可为空，由AI匹配</small></span><select name="order_id"><option value="">自动匹配订单</option>${orders.map(o=>`<option value="${esc(o.order_id)}" ${preselect===o.order_id?'selected':''}>${esc(o.order_no)} · ${esc(o.customer_name||'')}</option>`).join('')}</select></label><label class="field full"><span>消息原文</span><textarea name="raw_content" rows="10" placeholder="粘贴真实客户或工厂消息，不需要按固定模板填写。" required></textarea><small class="help">系统会立即创建后台识别任务；Coze执行期间页面不会被锁死。</small></label><label class="field"><span>消息时间</span><input name="source_time" type="datetime-local"></label></div><div class="boundary-note">AI不会直接修改订单、交期或任务。识别完成后，你需要在“人工确认”页面审核字段、风险和行动候选。</div><div class="form-actions"><button class="btn primary" type="submit" id="analyzeButton">开始后台识别</button></div></div></form><section class="panel"><div class="panel-head"><div><h2>本次处理结果</h2><p>后台任务会显示排队、处理中、完成或失败状态</p></div></div><div class="panel-body" id="intakeResult">${emptyState('等待消息输入','提交消息后，系统会在后台识别并生成候选。')}</div></section></section></div>`;
-  bindRouteButtons(root);const form=$('#intakeForm',root);form.source_time.value=new Date(Date.now()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16);
-  const renderCompleted=(r,body)=>{cache.dashboard=null;$('#intakeResult',root).innerHTML=`<div class="result-card"><div class="button-row"><span class="tag">${esc(automationLabel(r.workflow_source))}</span>${r.order_id?'<span class="tag">已关联订单</span>':'<span class="tag">待人工匹配</span>'}</div><h3>已生成AI候选</h3><p>候选尚未写入正式订单。可以进入人工确认，或把原始沟通转成任务候选。</p><div class="button-row" style="margin-top:14px"><button class="btn primary" data-review-now="${esc(r.review_id)}">进入人工确认</button><button class="btn bronze" id="intakeToTask">沟通转任务</button></div></div>`;$('#intakeResult [data-review-now]',root).onclick=()=>go(`confirm?review=${encodeURIComponent(r.review_id)}`);$('#intakeToTask',root).onclick=()=>{const selectedOrder=orders.find(o=>o.order_id===body.order_id);openCommunicationDrawer({mode:'ft05',order:selectedOrder||null,message:{raw_content:body.raw_content,sender_role:body.sender_role,source_channel:body.source_channel,source_message_id:r.message_id}})};toast('消息识别完成，等待人工确认','success')};
-  const pollJob=async(jobId,body,attempt=0)=>{if(!root.isConnected||route.name!=='intake')return;try{const j=await api(`/api/intake/jobs/${encodeURIComponent(jobId)}`,{timeoutMs:12000});if(j.status==='COMPLETED'){renderCompleted(j.result,body);return}if(j.status==='FAILED'){const message=j.error?.message||j.progress_message||'识别失败';$('#intakeResult',root).innerHTML=`<div class="result-card error-result"><h3>识别失败</h3><p>${esc(message)}</p><button class="btn" id="retryIntake">重新提交</button></div>`;$('#retryIntake',root).onclick=()=>form.requestSubmit();toast(message,'error');return}const elapsed=Math.max(0,Math.round((Date.now()-new Date(j.created_at).getTime())/1000));$('#intakeResult',root).innerHTML=`<div class="result-card processing-card"><div class="processing-line"><span class="inline-spinner"></span><div><h3>${j.status==='QUEUED'?'等待执行':'正在识别消息'}</h3><p>${esc(j.progress_message||'系统正在识别消息')} · 已等待 ${elapsed} 秒</p></div></div><div class="progress-hint">你可以切换到其他页面；后台任务不会因为离开本页而取消。</div></div>`;setTimeout(()=>pollJob(jobId,body,attempt+1),attempt<10?1200:2000)}catch(err){if(attempt<3)setTimeout(()=>pollJob(jobId,body,attempt+1),1800);else{$('#intakeResult',root).innerHTML=`<div class="result-card"><h3>状态查询失败</h3><p>${esc(err.message)}</p></div>`}}};
-  form.onsubmit=async e=>{e.preventDefault();const btn=$('#analyzeButton',root);const body=Object.fromEntries(new FormData(form));btn.disabled=true;btn.textContent='正在提交…';try{const job=await api('/api/intake/jobs',{method:'POST',body:JSON.stringify(body),timeoutMs:15000});$('#intakeResult',root).innerHTML=`<div class="result-card processing-card"><div class="processing-line"><span class="inline-spinner"></span><div><h3>已进入后台队列</h3><p>${esc(job.message||'正在准备识别消息')}</p></div></div><span class="tag">${esc(automationLabel(job.workflow_key))}</span></div>`;toast('已提交后台识别','success');pollJob(job.job_id,body)}catch(err){$('#intakeResult',root).innerHTML=`<div class="result-card"><h3>提交失败</h3><p>${esc(err.message)}</p></div>`;toast(err.message,'error')}finally{btn.disabled=false;btn.textContent='开始后台识别'}}
+  const orders=(await ordersData()).items;const preselect=route.query.order||'';
+  root.innerHTML=`<div class="page-stack"><div class="proto-2col"><form class="panel" id="intakeForm"><div class="panel-head"><h3>录入消息</h3><span class="tag muted">草稿</span></div><div class="panel-body"><div class="flow"><span class="step">① 粘贴消息</span><span class="arrow">→</span><span class="step">② 选择来源</span><span class="arrow">→</span><span class="step">③ 关联订单</span><span class="arrow">→</span><span class="step">④ AI 提取候选</span><span class="arrow">→</span><span class="step">⑤ 提交确认</span></div><label class="demo-note">消息来源</label><div class="src-pick"><button type="button" class="src-tag msg" data-sender-role="customer" style="padding:6px 12px">客户消息</button><button type="button" class="src-tag manual" data-sender-role="factory" style="padding:6px 12px;opacity:.55">工厂消息</button></div><input type="hidden" name="sender_role" value="customer"><label class="demo-note">消息渠道</label><select class="btn secondary" name="source_channel" style="height:36px;width:100%;text-align:left;margin:6px 0 12px"><option value="email">邮件</option><option value="wechat">企业微信</option><option value="internal">人工记录</option></select><label class="demo-note">关联订单</label><select class="btn secondary" name="order_id" style="height:36px;width:100%;text-align:left;margin:6px 0 12px"><option value="">由AI自动匹配</option>${orders.map(o=>`<option value="${esc(o.order_id)}" ${preselect===o.order_id?'selected':''}>${esc(o.order_no)} · ${esc(o.customer_name||'')}</option>`).join('')}</select><textarea class="btn" name="raw_content" style="width:100%;height:110px;text-align:left;padding:12px" placeholder="粘贴真实客户或工厂消息…" required></textarea><input type="hidden" name="source_time"><div class="row-actions" style="margin-top:12px"><button class="btn primary" type="submit" id="analyzeButton">分析消息</button></div></div></form><section class="panel"><div class="panel-head"><h3>变化候选</h3><span class="tag info" id="intakeState">等待输入</span></div><div class="panel-body" id="intakeResult">${emptyState('等待消息输入','系统只生成候选，不直接修改订单。')}</div></section></div></div>`;
+  const form=$('#intakeForm',root);form.source_time.value=new Date(Date.now()-new Date().getTimezoneOffset()*60000).toISOString().slice(0,16);$$('[data-sender-role]',root).forEach(b=>b.onclick=()=>{form.sender_role.value=b.dataset.senderRole;$$('[data-sender-role]',root).forEach(x=>x.style.opacity=x===b?'1':'.55')});
+  const renderCompleted=async(r,body)=>{cache.dashboard=null;let review=null;try{review=await api(`/api/reviews/${encodeURIComponent(r.review_id)}`)}catch{}const c=review?.candidate||{};const fields=(c.fields||[]).map(x=>fieldLabel(x.field_name)).join('、')||'等待人工查看';$('#intakeState',root).textContent='待确认';$('#intakeResult',root).innerHTML=`<p>AI已生成消息变化候选，尚未写入正式订单。</p><div class="cc-evidence">原文证据：${esc(body.raw_content)}</div><div class="run-row" style="margin-top:12px"><span class="r-label">影响字段</span><span class="r-val">${esc(fields)}</span></div><div class="run-row"><span class="r-label">提交后</span><span class="r-val">进入确认中心，不直接写回业务数据</span></div><div class="row-actions" style="margin-top:12px"><button class="btn primary" data-review-now="${esc(r.review_id)}">去确认中心</button><button class="btn ghost" id="intakeToTask">沟通转任务</button></div>`;$('#intakeResult [data-review-now]',root).onclick=()=>go(`confirm?review=${encodeURIComponent(r.review_id)}`);$('#intakeToTask',root).onclick=()=>{const selectedOrder=orders.find(o=>o.order_id===body.order_id);openCommunicationDrawer({mode:'ft05',order:selectedOrder||null,message:{raw_content:body.raw_content,sender_role:body.sender_role,source_channel:body.source_channel,source_message_id:r.message_id}})};toast('消息识别完成，等待人工确认','success')};
+  const pollJob=async(jobId,body,attempt=0)=>{if(!root.isConnected||route.name!=='intake')return;try{const j=await api(`/api/intake/jobs/${encodeURIComponent(jobId)}`,{timeoutMs:12000});if(j.status==='COMPLETED'){renderCompleted(j.result,body);return}if(j.status==='FAILED'){throw new Error(j.error?.message||j.progress_message||'识别失败')}$('#intakeState',root).textContent=j.status==='QUEUED'?'排队中':'分析中';$('#intakeResult',root).innerHTML=`<div class="loading-state compact"><span></span><p>${esc(j.progress_message||'系统正在识别消息')}</p></div>`;setTimeout(()=>pollJob(jobId,body,attempt+1),attempt<10?1200:2000)}catch(err){$('#intakeState',root).textContent='未完成';$('#intakeResult',root).innerHTML=`<p>${esc(err.message)}</p>`;toast(err.message,'error')}};
+  form.onsubmit=async e=>{e.preventDefault();const btn=$('#analyzeButton',root);const body=Object.fromEntries(new FormData(form));btn.disabled=true;btn.textContent='正在提交…';try{const job=await api('/api/intake/jobs',{method:'POST',body:JSON.stringify(body),timeoutMs:15000});$('#intakeState',root).textContent='排队中';$('#intakeResult',root).innerHTML='<div class="loading-state compact"><span></span><p>消息已进入后台识别队列</p></div>';pollJob(job.job_id,body)}catch(err){toast(err.message,'error')}finally{btn.disabled=false;btn.textContent='分析消息'}};
 }
 
 async function pageConfirm(root){
-  const d=await api('/api/reviews');let items=d.items.filter(x=>!search||[x.order_no,x.customer_name,x.raw_content,x.workflow_source].join(' ').toLowerCase().includes(search.toLowerCase()));selectedReviewId=route.query.review||selectedReviewId||items.find(x=>x.status==='PENDING')?.review_id||items[0]?.review_id;updateBadges(null,d.pending||0);root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>人工确认队列</h2><p>候选只有在人工确认并成功写回后，才会更新订单并重新排序</p></div><div class="button-row"><button class="btn" data-route="intake">接入新消息</button></div></div><section class="review-layout"><section class="panel"><div class="panel-head"><div><h3>候选队列</h3><p>${d.pending} 条待确认</p></div></div><div class="review-list" id="reviewList">${items.map(reviewCard).join('')||emptyState('暂无候选','接入一条真实消息后，AI候选会出现在这里。')}</div></section><section class="panel" id="reviewDetail">${emptyState('选择一条候选','查看原文证据、字段变化、风险和行动候选。')}</section></section></div>`;bindRouteButtons(root);$$('[data-review-id]',root).forEach(c=>c.onclick=()=>{selectedReviewId=c.dataset.reviewId;showReview(root,selectedReviewId)});if(selectedReviewId)await showReview(root,selectedReviewId)
-}
-function reviewCard(x){return `<article class="review-card ${selectedReviewId===x.review_id?'active':''}" data-review-id="${esc(x.review_id)}"><div class="top"><small>${esc(automationLabel(x.workflow_source))}</small>${statusBadge(x.status==='PENDING'?'NEEDS_CONFIRMATION':x.status==='CONFIRMED'?'DONE':'NOT_MY_RESPONSIBILITY')}</div><h3>${esc(x.order_no||'未关联订单')} · ${esc(x.customer_name||'待匹配')}</h3><p>${esc(x.raw_content||'导入的工作流候选')}</p></article>`}
-async function showReview(root,id){
-  const x=await api(`/api/reviews/${id}`),c=x.candidate||{};$$('.review-card',root).forEach(e=>e.classList.toggle('active',e.dataset.reviewId===id));const detail=$('#reviewDetail',root);detail.innerHTML=`<div class="panel-head"><div><h2>${esc(x.order_no||'待关联订单')}</h2><p>${esc(automationLabel(x.workflow_source))} · ${esc(fdt(x.created_at))}</p></div>${statusBadge(x.status==='PENDING'?'NEEDS_CONFIRMATION':x.status==='CONFIRMED'?'DONE':'NOT_MY_RESPONSIBILITY')}</div><div class="panel-body"><div class="boundary-note">确认前可以修改候选值。系统只有在正式写回成功后才更新订单和任务状态。</div><section class="candidate-section" style="margin-top:18px"><h3>原始消息</h3><div class="quote">${esc(x.raw_content||'无原文')}</div></section><section class="candidate-section"><h3>订单关联</h3><div class="candidate-row"><div><b>匹配状态</b><div class="source-quote">${esc(c.order_match?.status||'unknown')}</div></div><span>${esc(c.order_match?.matched_order_no||x.order_no||'—')}</span><span></span></div></section><section class="candidate-section"><h3>字段候选</h3><div id="candidateFields">${(c.fields||[]).map((f,i)=>`<div class="candidate-row"><div><b>${esc(fieldLabel(f.field_name))}</b><div class="source-quote">证据：${esc(f.source_quote||'—')}</div></div><input data-index="${i}" value="${esc(f.normalized_value??'')}"><span>${Math.round((f.confidence||0)*100)}%</span></div>`).join('')||'<p class="help">没有字段候选</p>'}</div></section><section class="candidate-section"><h3>风险提示</h3>${(c.risk_signals||[]).map(r=>`<div class="quote">${riskBadge(r.risk_level||'medium')} ${esc(r.type||'风险')}<br><small>${esc(r.evidence||'')}</small></div>`).join('')||'<p class="help">没有风险候选</p>'}</section><section class="candidate-section"><h3>行动候选</h3>${(c.action_candidates||[]).map(a=>`<div class="quote"><b>${esc(a.title||a.action_type||'行动')}</b><br>${esc(a.recommended_action||'')}</div>`).join('')||'<p class="help">没有行动候选</p>'}</section>${x.status==='PENDING'?`<div class="button-row"><button class="btn primary" id="confirmReview">确认并写回</button><button class="btn danger" id="rejectReview">驳回候选</button></div>`:`<div class="button-row"><button class="btn" data-go="orders/${esc(x.order_id)}">查看订单</button></div>`}</div>`;bindRouteButtons(detail);if(x.status!=='PENDING')return;$('#confirmReview',detail).onclick=async()=>{const btn=$('#confirmReview',detail);const edited=JSON.parse(JSON.stringify(c));$$('#candidateFields input',detail).forEach(inp=>{const f=edited.fields[Number(inp.dataset.index)];let v=inp.value;if(['current_progress','current_progress_percentage'].includes(f.field_name))v=Number(v);f.normalized_value=v});btn.disabled=true;btn.textContent='正在写回正式数据…';try{const r=await api(`/api/reviews/${id}/confirm`,{method:'POST',body:JSON.stringify({candidate:edited,operator_id:currentUser()}),timeoutMs:240000});cache={operators:cache.operators};toast('确认已写回，行动排序已更新','success');go(`orders/${r.order_id}`)}catch(e){toast(e.message,'error')}finally{btn.disabled=false;btn.textContent='确认并写回'}};$('#rejectReview',detail).onclick=async()=>{try{await api(`/api/reviews/${id}/reject`,{method:'POST',body:JSON.stringify({operator_id:currentUser()})});selectedReviewId=null;toast('候选已驳回','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
+  const role=currentUser()==='MANAGER-1'?'manager':'operator';const [reviews,agent]=await Promise.all([api('/api/reviews'),api(`/api/agent/overview?current_user_id=${encodeURIComponent(currentUser())}&current_role=${role}`).catch(()=>({candidates:[],approvals:[],summary:{}}))]);
+  const reviewItems=(reviews.items||[]).filter(x=>!search||[x.order_no,x.customer_name,x.raw_content,x.workflow_source].join(' ').toLowerCase().includes(search.toLowerCase()));
+  const anomalies=(agent.candidates||[]).filter(x=>!search||[x.order_no,x.customer_name,x.recommended_action].join(' ').toLowerCase().includes(search.toLowerCase()));
+  const approvals=(agent.approvals||[]).filter(x=>!search||[x.order_no,x.action_type,x.approval_id].join(' ').toLowerCase().includes(search.toLowerCase()));
+  const pending=reviewItems.filter(x=>x.status==='PENDING').length+anomalies.filter(x=>['ANOMALY_CANDIDATE','PENDING_CONFIRMATION'].includes(x.status)).length+approvals.filter(x=>x.status==='PENDING').length;
+  const confirmed=reviewItems.filter(x=>x.status==='CONFIRMED').length+anomalies.filter(x=>x.status==='CONFIRMED').length+approvals.filter(x=>x.status==='APPROVED').length;
+  const rejected=reviewItems.filter(x=>x.status==='REJECTED').length+anomalies.filter(x=>x.status==='REJECTED').length+approvals.filter(x=>x.status==='REJECTED').length;updateBadges(null,pending);
+  const cards=[...reviewItems.map(confirmReviewCard),...anomalies.map(confirmAnomalyCard),...approvals.map(confirmApprovalCard)];
+  root.innerHTML=`<div class="page-stack"><div class="metric-grid">${uiMetric('待确认',pending,'red','review')}${uiMetric('消息候选',reviewItems.filter(x=>x.status==='PENDING').length,'blue','message')}${uiMetric('已确认',confirmed,'green','check')}${uiMetric('已驳回',rejected,'muted','close')}</div><section class="panel"><div class="panel-head"><h3>确认队列</h3><span class="tag danger">${pending} 项待处理</span></div><div class="panel-body"><div class="confirm-filters"><button class="confirm-filter active" data-confirm-filter="全部">全部</button><button class="confirm-filter" data-confirm-filter="数据变更">数据变更</button><button class="confirm-filter" data-confirm-filter="任务草稿">任务草稿</button><button class="confirm-filter" data-confirm-filter="异常确认">异常确认</button><button class="confirm-filter" data-confirm-filter="对外动作">对外动作</button></div><div class="confirm-list">${cards.join('')||emptyState('暂无待确认事项','消息字段变更、异常候选和Agent审批会统一进入这里。')}</div></div></section></div>`;
+  $$('[data-confirm-filter]',root).forEach(filter=>filter.onclick=()=>{const type=filter.dataset.confirmFilter;$$('[data-confirm-filter]',root).forEach(x=>x.classList.toggle('active',x===filter));$$('[data-confirm-type]',root).forEach(card=>card.hidden=type!=='全部'&&card.dataset.confirmType!==type)});
+  $$('[data-review-open]',root).forEach(b=>b.onclick=()=>openReviewEditor(b.dataset.reviewOpen));
+  $$('[data-review-confirm]',root).forEach(b=>b.onclick=()=>confirmReviewDirect(b.dataset.reviewConfirm));
+  $$('[data-review-reject]',root).forEach(b=>b.onclick=()=>rejectReviewDirect(b.dataset.reviewReject));
+  $$('[data-anomaly-confirm]',root).forEach(b=>b.onclick=()=>decideAnomaly(b.dataset.anomalyConfirm,'CONFIRM'));
+  $$('[data-anomaly-reject]',root).forEach(b=>b.onclick=()=>decideAnomaly(b.dataset.anomalyReject,'REJECT'));
+  $$('[data-approval-approve]',root).forEach(b=>b.onclick=()=>decideApproval(b.dataset.approvalApprove,'APPROVE'));
+  $$('[data-approval-reject]',root).forEach(b=>b.onclick=()=>decideApproval(b.dataset.approvalReject,'REJECT'));
+  if(route.query.review)setTimeout(()=>openReviewEditor(route.query.review),0);
 }
 function fieldLabel(name){return({packaging_method:'包装方式',requested_delivery_date:'客户正式交期',latest_supplier_commitment:'工厂完成承诺',current_progress:'当前进度',current_node:'当前节点',customer_name:'客户名称',product_name:'产品名称'}[name]||name)}
 
@@ -280,133 +321,85 @@
 
 async function pageAgent(root){
   const role=currentUser()==='MANAGER-1'?'manager':'operator';
-  const [data,status]=await Promise.all([
+  let conversations=loadAgentConversations();if(!conversations.length){conversations=[newAgentConversation()];saveAgentConversations(conversations)}
+  let activeId=localGet(agentActiveKey())||conversations[0].id;let active=conversations.find(x=>x.id===activeId)||conversations[0];localSet(agentActiveKey(),active.id);
+  const [data,status,job,trace]=await Promise.all([
     api(`/api/agent/overview?current_user_id=${encodeURIComponent(currentUser())}&current_role=${role}`),
-    api('/api/agent/status')
+    api('/api/agent/status'),
+    active.last_job_id?api(`/api/agent/chat/jobs/${encodeURIComponent(active.last_job_id)}`).catch(()=>null):Promise.resolve(null),
+    active.last_run_id?api(`/api/agent/runs/${encodeURIComponent(active.last_run_id)}/trace?current_user_id=${encodeURIComponent(currentUser())}&current_role=${role}`).catch(()=>null):Promise.resolve(null)
   ]);
   updateBadges(null,null,data.summary.candidate_count);
-  const latest=data.reports?.[0]?.report||null;
-  const latestItems=latest?.top_items||[];
-  const latestGaps=latest?.information_gaps||[];
-  const cozeConfigured=!!status.coze_agent?.configured;
-  const scheduleConfigured=!!status.daily_schedule?.configured;
-  const latestRun=data.latest_run;
-  const latestCalls=data.latest_tool_calls||[];
-  const maxCalls=Number(status.limits?.max_tool_calls||8),maxSeconds=Number(status.limits?.max_duration_seconds||120);
-  const currentMetrics=latest?[
-    ['扫描订单',Number(latest.screened_order_count||0),'green','orders','本次运行'],
-    ['风险订单',Number(latest.risk_order_count??latestItems.length),'red','alert','最多7笔不同订单'],
-    ['异常信号',Number((latest.anomaly_signal_count??latest.anomaly_candidate_count) || 0),'amber','alert','同一订单可含多信号'],
-    ['信息缺口',Number(latest.information_gap_order_count??latestGaps.length),'blue','review','单独展示，不进风险榜']
-  ]:[['扫描订单',0,'green','orders','尚未运行'],['风险订单',0,'red','alert','尚未运行'],['异常信号',0,'amber','alert','尚未运行'],['信息缺口',0,'blue','review','尚未运行']];
-  root.innerHTML=`<div class="page-stack">
-    <section class="agent-hero"><div><span>FLOWORDER AGENT · HUMAN IN THE LOOP</span><h2>Agent理解目标、选择技能，确定性规则与人工审批共同兜底</h2><p>支持跨订单异常巡检、单订单证据解释和批量进展解析。Coze Agent与规则巡检是两种明确模式，不会静默互相冒充。</p></div><div class="agent-mode-chip ${cozeConfigured?'online':'offline'}"><i></i><div><small>当前Agent模式</small><strong>${cozeConfigured?'Coze Agent已配置':'规则巡检可用'}</strong></div></div></section>
-    <section class="agent-connection-grid">
-      ${agentConnectionCard('后端服务','正常',`FlowOrder v${status.version}`,'green')}
-      ${agentConnectionCard('Coze Agent',cozeConfigured?'已配置':'未配置',cozeConfigured?'可启动动态工具编排':'需配置Token与Bot ID',''+(cozeConfigured?'green':'amber'))}
-      ${agentConnectionCard('定时巡检',scheduleConfigured?'接口已配置':'未配置',scheduleConfigured?'计划每天08:30；需外部调度器触发':'手动规则巡检仍可使用',scheduleConfigured?'green':'blue')}
-    </section>
-    <section class="panel agent-chat-panel"><div class="panel-head"><div><h2>向FlowOrder Agent发起业务目标</h2><p>当前诊断身份：${esc(operatorNames[currentUser()]||currentUser())} · ${role==='manager'?'业务主管｜团队订单':'跟单专员｜本人负责订单'}。用户只描述业务目标，不需要填写系统内部用户ID。</p></div><button class="btn" id="showAgentPolicy">查看自治边界</button></div>
-      <div class="agent-goal-layout"><div><textarea id="agentQuestion" rows="3">检查我未来14天内最需要处理的订单。</textarea><div class="agent-shortcuts"><button type="button" data-agent-prompt="检查我未来14天内最需要处理的订单。">近期风险巡检</button><button type="button" data-agent-prompt="解释最高优先级订单为什么需要先处理。">解释排序原因</button><button type="button" data-agent-prompt="检查是否存在已经超过承诺回复时间的事项。">检查超时承诺</button></div></div>
-        <div class="agent-options"><label><span>巡检范围</span><select id="agentDueDays"><option value="7">未来7天</option><option value="14" selected>未来14天</option><option value="30">未来30天</option></select></label><label><span>最多返回</span><select id="agentTopN"><option value="3">3笔</option><option value="5">5笔</option><option value="7" selected>7笔</option></select></label><label class="agent-check"><input type="checkbox" id="agentCreateTask" checked><span>为最高优先级生成任务草稿</span></label><label class="agent-check"><input type="checkbox" id="agentCreateApproval" checked><span>需要正式动作时创建审批</span></label></div></div>
-      <div class="button-row agent-run-buttons"><button class="btn primary" id="askAgent" ${cozeConfigured?'':'disabled'}>启动Agent诊断</button><button class="btn" id="runAgentInspection">仅运行规则巡检</button><span class="agent-mode-help">${cozeConfigured?'标准巡检采用两次工具调用快速链路；Agent仍会按目标选择组合工具或原子工具。':'Coze未配置，Agent按钮已禁用；规则巡检不会被标记为Agent结果。'}</span></div><div id="agentAnswer" class="agent-answer" hidden></div>
-    </section>
-    <section><div class="section-label-row"><div><h2>本次运行</h2><p>${latest?`${esc(latest.generated_at||'')} · ${latest.execution_mode==='RULE_INSPECTION'?'规则巡检':'Agent诊断'}`:'尚无巡检结果'}</p></div></div><div class="metric-grid">${currentMetrics.map(x=>metric(...x)).join('')}</div></section>
-    <section class="panel"><div class="panel-head"><div><h2>可审计执行轨迹</h2><p>展示目标路由、工具调用、真实ID与停止原因，不展示模型私有推理。</p></div>${latestRun?`<span class="status">${/RULE/.test(latestRun.trigger_type||'')?'规则模式':'Agent模式'}</span>`:''}</div><div class="panel-body">${agentRunTrace(latestRun,latestCalls)}</div></section>
-    ${latest?`<section class="panel"><div class="panel-head"><div><h2>最近一次风险订单 Top ${latestItems.length}</h2><p>筛选${Number(latest.screened_order_count||0)}笔 · ${Number((latest.anomaly_signal_count??latest.anomaly_candidate_count) || 0)}个异常信号 · 不足7笔不补齐</p></div><span class="status">人工确认后才生效</span></div><div class="agent-candidate-grid">${latestItems.map(agentCandidateCard).join('')||emptyState('本次没有真实风险订单','系统不会用信息缺口凑满Top 7。')}</div></section>`:''}
-    ${latestGaps.length?`<section class="panel information-gap-panel"><div class="panel-head"><div><h2>本次信息缺口</h2><p>这些订单需要补充事实，但不属于风险Top 7。</p></div><span class="tag">${latestGaps.length}笔</span></div><div class="information-gap-list">${latestGaps.map(g=>`<article><div><strong>${esc(g.order_no||g.order_id)}</strong><small>${esc((g.missing_information||[]).join('、')||'缺少动态信息')}</small></div><button class="btn small soft" data-go="orders/${esc(g.order_id)}">补充信息</button></article>`).join('')}</div></section>`:''}
-    <section><div class="section-label-row"><div><h2>历史累计</h2><p>历史存量与本次运行分开统计，避免把累计候选误认为本次Top 7。</p></div></div><div class="metric-grid">${metric('累计风险候选',data.summary.candidate_count,'red','alert','待确认或处理中')}${metric('累计信息缺口',data.summary.information_gap_count,'amber','review','不进入风险榜')}${metric('待审批动作',data.summary.pending_approval_count,'blue','review','尚未正式执行')}${metric('巡检报告',data.summary.report_count,'green','chart','历史报告数')}</div></section>
-    <section class="panel"><div class="panel-head"><div><h2>历史风险候选</h2><p>正式异常、任务、写回与外部发送仍受人工审批控制。</p></div></div><div class="agent-candidate-grid">${data.candidates.map(agentCandidateCard).join('')||emptyState('还没有风险候选','启动Agent诊断或规则巡检后，真实风险会出现在这里。')}</div></section>
-    <section class="panel"><div class="panel-head"><div><h2>待审批动作</h2><p>高风险动作必须等待人工；系统不会自动发送客户或工厂消息。</p></div></div><div class="approval-list">${data.approvals.filter(x=>x.status==='PENDING').map(a=>`<article><div><strong>${esc(a.action_type)}</strong><small>${esc(a.order_no||a.order_id||'')} · ${esc(a.approval_id)}</small></div><span class="status NEEDS_CONFIRMATION">${esc(a.required_role==='manager'?'需要主管':'需要人工')}</span></article>`).join('')||emptyState('当前没有待审批动作','Agent生成的正式动作会进入这里。')}</div></section>
-  </div>`;
-  $$('[data-agent-prompt]',root).forEach(b=>b.onclick=()=>{$('#agentQuestion',root).value=b.dataset.agentPrompt});
-  const agentButton=$('#askAgent',root),agentBox=$('#agentAnswer',root);
-  const renderAgentJobResult=(job)=>{
-    const r=job.result||{},runId=r.run?.run_id||job.linked_run_id||'';
-    const identity=r.resolved_identity||{},identityText=identity.current_user_name?`${identity.current_user_name} · ${identity.current_role==='manager'?'团队订单':'本人负责订单'}`:`${operatorNames[currentUser()]||currentUser()}`;
-    agentBox.hidden=false;
-    const perf=r.performance_metrics||{},perfText=perf.tool_call_count!=null?` · Agent工具 ${Number(perf.tool_call_count)} 次 · 后端工具 ${Number(perf.backend_tool_duration_ms||0)}ms`:'';
-    agentBox.innerHTML=`<strong>Coze Agent诊断结果</strong><p class="agent-identity-note">已按当前身份：${esc(identityText)}</p><pre>${esc(r.answer||'Agent已完成工具执行，但未返回文本总结；请刷新查看执行轨迹和诊断结果。')}</pre><small>后台耗时 ${(Number(job.duration_ms||r.duration_ms||0)/1000).toFixed(1)}s${perfText}${runId?` · run_id: ${esc(runId)}`:''}${r.conversation_id?' · 已保留Coze会话':''}</small><p class="agent-no-fallback">本结果来自Coze Agent；页面轮询不会重复提交目标，也不会静默替换成规则巡检结果。</p>`;
-    toast('Agent后台运行完成，可刷新查看执行轨迹','success');
-  };
-  const pollAgentJob=async(jobId,startedAt=Date.now(),attempt=0)=>{
-    if(!root.isConnected||route.name!=='agent')return;
-    try{
-      const job=await api(`/api/agent/chat/jobs/${encodeURIComponent(jobId)}`,{timeoutMs:15000});
-      if(job.status==='COMPLETED'){
-        sessionSet('floworderAgentJobId','');
-        agentButton.disabled=!cozeConfigured;agentButton.textContent='启动Agent诊断';
-        renderAgentJobResult(job);return;
-      }
-      if(job.status==='FAILED'){
-        sessionSet('floworderAgentJobId','');
-        agentButton.disabled=!cozeConfigured;agentButton.textContent='启动Agent诊断';
-        agentBox.hidden=false;agentBox.innerHTML=`<strong>Coze Agent后台任务未完成</strong><p>${esc(job.error_message||'未知错误')}</p><p>本次没有自动切换为规则结果，也无需重复提交相同目标。</p>`;
-        toast(job.error_message||'Agent后台任务失败','error');return;
-      }
-      const elapsed=Math.max(0,Math.round((Date.now()-startedAt)/1000));
-      agentBox.hidden=false;
-      agentBox.innerHTML=`<div class="loading-state compact"><span></span><div><strong>${esc(job.message||'Agent正在后台执行')}</strong><p>已等待 ${elapsed} 秒 · job_id: ${esc(jobId)}</p><small>你可以切换到其他页面；后台任务不会取消，也不要重复点击。</small></div></div>`;
-      agentButton.disabled=true;agentButton.textContent='Agent后台运行中…';
-      setTimeout(()=>pollAgentJob(jobId,startedAt,attempt+1),attempt<15?1800:3000);
-    }catch(e){
-      const elapsed=Math.max(0,Math.round((Date.now()-startedAt)/1000));
-      agentBox.hidden=false;agentBox.innerHTML=`<strong>正在恢复Agent运行状态</strong><p>${esc(e.message)}</p><small>后台任务可能仍在执行，系统将在3秒后继续查询。已等待 ${elapsed} 秒。</small>`;
-      if(attempt<60)setTimeout(()=>pollAgentJob(jobId,startedAt,attempt+1),3000);else{agentButton.disabled=!cozeConfigured;agentButton.textContent='启动Agent诊断'}
-    }
-  };
-  agentButton.onclick=async()=>{
-    const q=$('#agentQuestion',root).value.trim();
-    if(!q)return toast('请输入诊断目标','error');
-    agentButton.disabled=true;agentButton.textContent='正在提交后台任务…';agentBox.hidden=false;
-    agentBox.innerHTML='<div class="loading-state compact"><span></span><p>正在创建Agent后台任务…</p></div>';
-    try{
-      const payload={question:q,current_user_id:currentUser(),current_role:role,due_within_days:Number($('#agentDueDays',root).value),top_n:Number($('#agentTopN',root).value),create_task_draft:$('#agentCreateTask',root).checked,create_approval_request:$('#agentCreateApproval',root).checked};
-      const job=await api('/api/agent/chat/jobs',{method:'POST',body:JSON.stringify(payload),timeoutMs:15000});
-      sessionSet('floworderAgentJobId',job.job_id);
-      toast('Agent任务已进入后台，不会因页面请求超时而中断','success');
-      pollAgentJob(job.job_id,Date.now());
-    }catch(e){
-      agentBox.innerHTML=`<strong>Agent任务提交失败</strong><p>${esc(e.message)}</p><p>任务尚未进入后台，可以确认服务状态后重新点击。</p>`;
-      toast(e.message,'error');agentButton.disabled=!cozeConfigured;agentButton.textContent='启动Agent诊断';
-    }
-  };
-  const pendingAgentJobId=sessionGet('floworderAgentJobId');
-  if(pendingAgentJobId){agentBox.hidden=false;agentButton.disabled=true;agentButton.textContent='Agent后台运行中…';pollAgentJob(pendingAgentJobId,Date.now())}
-  $('#runAgentInspection',root).onclick=async()=>{
-    const b=$('#runAgentInspection',root),days=Number($('#agentDueDays',root).value),topN=Number($('#agentTopN',root).value);
-    b.disabled=true;b.textContent='规则巡检中…';
-    try{await api('/api/agent/inspection/run',{method:'POST',body:JSON.stringify({current_user_id:currentUser(),current_role:role,due_within_days:days,top_n:topN,goal:`规则巡检未来${days}天订单`,trigger_type:'MANUAL_RULE'}),timeoutMs:120000});cache={operators:cache.operators};toast('规则巡检完成；结果已明确标记为规则模式','success');renderRoute(false)}catch(e){toast(e.message,'error')}finally{b.disabled=false;b.textContent='仅运行规则巡检'}
-  };
-  $('#showAgentPolicy',root).onclick=()=>openInfoModal('Agent自治边界',`<div class="page-stack"><div class="boundary-note"><strong>Agent自主完成</strong><p>理解开放目标、选择组合或原子工具、根据结果追问/停止、生成任务草稿与审批请求。</p></div><div class="boundary-note"><strong>后端确定性规则</strong><p>日期比较、权限、去重、同订单异常聚合、信息缺口过滤、Top N与幂等写入。</p></div><div class="boundary-note danger"><strong>人工承担业务责任</strong><p>正式修改订单、接受延期、费用赔偿、外发消息和高风险放行。</p></div><p>运行预算由后端返回：最多${maxCalls}次工具调用、${maxSeconds}秒；达到预算、证据不足或需要审批时停止。</p></div>`);
-  $$('[data-anomaly-confirm]',root).forEach(b=>b.onclick=()=>decideAnomaly(b.dataset.anomalyConfirm,'CONFIRM'));
-  $$('[data-anomaly-reject]',root).forEach(b=>b.onclick=()=>decideAnomaly(b.dataset.anomalyReject,'REJECT'));
-  $$('[data-anomaly-resolve]',root).forEach(b=>b.onclick=()=>decideAnomaly(b.dataset.anomalyResolve,'RESOLVE'));
-  bindRouteButtons(root);
+  const latest=data.reports?.[0]?.report||null;const latestRun=trace?.run||data.latest_run;const latestCalls=trace?.tool_calls||data.latest_tool_calls||[];const cozeConfigured=!!status.coze_agent?.configured;
+  if(job?.status==='COMPLETED'&&active.last_processed_job_id!==job.job_id){const answer=job.result?.answer||'Agent已完成运行，请查看右侧结构化结果。';active.messages.push({role:'assistant',content:answer,time:job.completed_at||new Date().toISOString()});active.coze_conversation_id=job.conversation_id||job.result?.conversation_id||active.coze_conversation_id;active.last_run_id=job.linked_run_id||job.result?.run?.run_id||active.last_run_id;active.last_processed_job_id=job.job_id;active.updated_at=job.completed_at||new Date().toISOString();saveOneConversation(active);conversations=loadAgentConversations()}
+  if(latestRun?.run_id&&active.last_run_id===latestRun.run_id&&latest){active.metrics={screened:Number(latest.screened_order_count||0),risk:Number(latest.risk_order_count||0),gaps:Number(latest.information_gap_order_count||0)};saveOneConversation(active)}
+  const metrics=active.metrics||{screened:Number(latest?.screened_order_count||0),risk:Number(latest?.risk_order_count||0),gaps:Number(latest?.information_gap_order_count||0)};
+  root.innerHTML=`<div class="page-stack"><div class="agent-3col">
+    <section class="panel" style="display:flex;flex-direction:column"><div class="panel-head"><button class="btn primary" id="newAgentConversation" style="height:30px;padding:0 10px">+ 新建会话</button></div><div class="panel-body"><div class="session-list" id="agentSessionList">${conversations.map(c=>agentSessionItem(c,c.id===active.id)).join('')}</div></div></section>
+    <section class="panel" style="display:flex;flex-direction:column"><div class="panel-head"><h3>对话与目标</h3></div><div class="panel-body" style="flex:1;display:flex;flex-direction:column"><div class="chat" id="agentChat">${agentMessagesHtml(active.messages)}</div><div id="agentLiveStatus" class="demo-note" style="margin-top:auto"></div><div class="chat-input"><input id="agentQuestion" type="text" placeholder="输入目标，例如：检查未来14天最需要处理的订单" ${cozeConfigured?'':'disabled'} /><button class="btn primary" id="askAgent" ${cozeConfigured?'':'disabled'}>发送</button></div></div></section>
+    <section class="panel" style="display:flex;flex-direction:column"><div class="panel-head"><h3>本次运行结果</h3><span class="tag ${job?.status==='FAILED'?'danger':job?.status==='RUNNING'||job?.status==='QUEUED'?'warning':'success'}">${esc(job?.status==='RUNNING'?'运行中':job?.status==='QUEUED'?'排队中':job?.status==='FAILED'?'未完成':active.last_run_id?'已完成':'暂无运行')}</span></div><div class="panel-body" id="agentResultPanel">${agentStructuredResultHtml({active,job,latest,latestRun,latestCalls,status,metrics})}</div></section>
+  </div></div>`;
+  $('#newAgentConversation',root).onclick=()=>{const list=loadAgentConversations();const c=newAgentConversation();list.unshift(c);saveAgentConversations(list);localSet(agentActiveKey(),c.id);renderRoute(false)};
+  $$('[data-agent-session]',root).forEach(b=>b.onclick=()=>{localSet(agentActiveKey(),b.dataset.agentSession);renderRoute(false)});
+  const send=async()=>{const input=$('#agentQuestion',root),question=input.value.trim();if(!question)return toast('请输入业务目标','error');active=loadAgentConversations().find(x=>x.id===active.id)||active;active.messages.push({role:'user',content:question,time:new Date().toISOString()});if(!active.title||active.title==='新会话')active.title=question.slice(0,24);active.updated_at=new Date().toISOString();saveOneConversation(active);$('#agentChat',root).innerHTML=agentMessagesHtml(active.messages);input.value='';$('#askAgent',root).disabled=true;$('#agentLiveStatus',root).textContent='正在创建Agent后台任务…';try{const created=await api('/api/agent/chat/jobs',{method:'POST',body:JSON.stringify({question,current_user_id:currentUser(),current_role:role,due_within_days:14,top_n:7,create_task_draft:true,create_approval_request:true,conversation_id:active.coze_conversation_id||null}),timeoutMs:15000});active.last_job_id=created.job_id;active.updated_at=new Date().toISOString();saveOneConversation(active);pollConversationJob(root,active.id,created.job_id)}catch(e){$('#agentLiveStatus',root).textContent=e.message;$('#askAgent',root).disabled=false;toast(e.message,'error')}};
+  $('#askAgent',root).onclick=send;$('#agentQuestion',root).onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}};
+  if(job&&['QUEUED','RUNNING'].includes(job.status))pollConversationJob(root,active.id,job.job_id);
+  $('#runRuleInspection',root)?.addEventListener('click',async()=>{const b=$('#runRuleInspection',root);b.disabled=true;try{await api('/api/agent/inspection/run',{method:'POST',body:JSON.stringify({current_user_id:currentUser(),current_role:role,due_within_days:14,top_n:7,goal:'规则巡检未来14天订单',trigger_type:'MANUAL_RULE'}),timeoutMs:120000});cache={operators:cache.operators};toast('规则巡检完成','success');renderRoute(false)}catch(e){toast(e.message,'error')}finally{b.disabled=false}});
+  bindRouteButtons(root)
 }
 
 async function decideAnomaly(id,decision){try{await api(`/api/agent/candidates/${encodeURIComponent(id)}/decision`,{method:'POST',body:JSON.stringify({decision,operator_id:currentUser(),current_role:currentUser()==='MANAGER-1'?'manager':'operator'})});toast(decision==='CONFIRM'?'异常已由人工确认':decision==='REJECT'?'候选已驳回':'异常已标记解决','success');renderRoute(false)}catch(e){toast(e.message,'error')}}
 function openInfoModal(title,html){openModal({eyebrow:'AGENT POLICY',title,subtitle:'受控型Agent不会绕过人工责任边界',body:html,actions:'<button class="btn primary" data-close-modal>知道了</button>'});$$('[data-close-modal]',$('#modal')).forEach(b=>b.onclick=closeModal)}
 
 async function pageManage(root){
-  if(currentUser()!=='MANAGER-1'){root.innerHTML=`<section class="panel">${emptyState('当前身份没有团队管理权限','跟单人员只查看本人订单与任务；切换为主管身份后可查看团队负载。',`<button class="btn primary" data-route="today">返回我的行动</button>`)}</section>`;return}
-  const d=await api('/api/management');const maxWork=Math.max(1,...d.workload.map(x=>x.total));const maxState=Math.max(1,...Object.values(d.state_distribution||{}));root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>管理视图</h2><p>聚焦团队负载、风险和等待瓶颈，不展示装饰性演示数据</p></div><span class="tag">更新于 ${esc(fdt(d.generated_at))}</span></div><section class="metric-grid">${metric('立即处理',d.summary.do_now,'red','alert')}${metric('今天处理',d.summary.do_today,'amber','today')}${metric('等待外部',d.summary.waiting,'blue','clock')}${metric('主管介入',d.summary.escalate,'green','user')}</section><section class="chart-grid"><section class="panel"><div class="panel-head"><div><h2>团队负载</h2><p>开放任务、紧急任务和等待任务</p></div></div><div class="panel-body"><div class="bar-list">${d.workload.map(w=>`<div class="bar-row"><span>${esc(w.name)}</span><div class="bar-track"><i style="width:${Math.round(w.total/maxWork*100)}%"></i></div><b>${w.total}</b></div>`).join('')||emptyState('暂无负载','当前没有任务数据。')}</div></div></section><section class="panel"><div class="panel-head"><div><h2>行动状态分布</h2><p>从状态机看团队当前瓶颈</p></div></div><div class="panel-body"><div class="bar-list">${Object.entries(d.state_distribution||{}).map(([s,n])=>`<div class="bar-row"><span>${esc(stateLabels[s]||s)}</span><div class="bar-track"><i style="width:${Math.round(n/maxState*100)}%"></i></div><b>${n}</b></div>`).join('')||emptyState('暂无状态数据','当前没有任务数据。')}</div></div></section></section><section class="panel"><div class="panel-head"><div><h2>需要主管关注</h2><p>严重风险或已升级任务</p></div></div><div class="action-list">${d.escalations.length?d.escalations.map(actionRow).join(''):emptyState('暂无升级事项','当前没有严重风险或主管介入任务。')}</div></section></div>`;bindTaskActions(root,d.escalations);bindRouteButtons(root)
+  if(currentUser()!=='MANAGER-1'){root.innerHTML=`<section class="panel">${emptyState('当前身份没有团队管理权限','跟单专员只查看本人订单与任务。',`<button class="btn primary" data-route="today">返回今日工作台</button>`)}</section>`;return}
+  const d=await api('/api/management');const maxWork=Math.max(1,...(d.workload||[]).map(x=>x.total));const maxState=Math.max(1,...Object.values(d.state_distribution||{}));
+  root.innerHTML=`<div class="page-stack">
+    <div class="metric-grid">${uiMetric('立即处理',d.summary.do_now,'red','alert')}${uiMetric('今天处理',d.summary.do_today,'amber','today')}${uiMetric('等待外部',d.summary.waiting,'blue','clock')}${uiMetric('主管介入',d.summary.escalate,'red','user')}</div>
+    <h4 class="section-title">团队状态</h4>
+    <div class="proto-2col">
+      <section class="panel"><div class="panel-head"><h3>行动状态分布</h3><span class="tag info">实时</span></div><div class="panel-body"><div class="bar-list">${Object.entries(d.state_distribution||{}).map(([s,n])=>`<div class="bar-row"><span>${esc(stateLabels[s]||s)}</span><div class="bar-track"><i style="width:${Math.round(n/maxState*100)}%"></i></div><b>${n}</b></div>`).join('')||emptyState('暂无状态数据','当前没有任务数据。')}</div></div></section>
+      <section class="panel"><div class="panel-head"><h3>人员负载</h3></div><div class="panel-body"><div class="bar-list">${(d.workload||[]).map(w=>`<div class="bar-row"><span>${esc(w.name)}</span><div class="bar-track"><i style="width:${Math.round(w.total/maxWork*100)}%"></i></div><b>${w.total}</b></div>`).join('')||emptyState('暂无负载','当前没有团队任务。')}</div></div></section>
+    </div>
+    <h4 class="section-title">积压预警</h4>
+    <section class="panel"><div class="panel-head"><h3>需要主管关注</h3><span class="tag danger">${(d.escalations||[]).length} 项</span></div><div class="panel-body">${(d.escalations||[]).map(t=>uiActionRow(t,'查看')).join('')||emptyState('暂无升级事项','当前没有严重风险或主管介入任务。')}</div></section>
+  </div>`;
+  bindTaskActions(root,(d.escalations||[]).map(t=>({...t,order:t.order||{}})));bindRouteButtons(root)
 }
 
 async function pageSettings(root){
   const isManager=currentUser()==='MANAGER-1';
-  const [s,ops]=await Promise.all([api(`/api/settings?user_id=${currentUser()}`),api('/api/operators')]);
+  const [s,ops,agentStatus,importStatus,storage,cozeStatus]=await Promise.all([
+    api(`/api/settings?user_id=${encodeURIComponent(currentUser())}`),
+    api('/api/operators'),
+    api('/api/agent/status').catch(()=>({})),
+    api('/api/import/capabilities').catch(()=>({})),
+    api('/api/system/storage').catch(()=>({})),
+    api('/api/coze/status').catch(()=>({}))
+  ]);
   settings=s.settings||settings;cache.operators=ops.items||cache.operators||[];
-  let managerContent='';
-  if(isManager){
-    const [coze,comm,imp,storage]=await Promise.all([api('/api/coze/status'),api('/api/communication/capabilities'),api('/api/import/capabilities'),api('/api/system/storage')]);
-    managerContent=`<section class="panel"><div class="panel-head"><div><h2>连接状态</h2><p>仅主管可查看系统连接、数据持久化和导入能力</p></div></div><div class="panel-body"><div class="integration-list">${integrationItem('主服务','FastAPI / SQLite','已连接',true)}${integrationItem('数据持久化',storage.on_persistent_path?storage.db_path:(storage.warning||storage.db_path),storage.on_persistent_path?'已持久化':'需配置',storage.on_persistent_path)}${integrationItem('订单识别与行动排序',coze.ready?'核心自动化已配置':'配置不完整',coze.ready?'已连接':'需检查',coze.ready)}${integrationItem('沟通转任务',comm.workflows?.ft05?.configured?'自动化已配置':'缺少配置',comm.workflows?.ft05?.configured?'已连接':'未配置',comm.workflows?.ft05?.configured)}${integrationItem('受控沟通草稿',comm.workflows?.ft06?.configured?'自动化已配置':'缺少配置',comm.workflows?.ft06?.configured?'已连接':'未配置',comm.workflows?.ft06?.configured)}${integrationItem('Excel/CSV导入',`${imp.supported_formats?.join(' / ')} · 最多${imp.max_rows}行`,'可用',true)}</div><div class="button-row" style="margin-top:14px"><button class="btn" id="communicationKey">设置沟通操作密钥</button><a class="btn" href="/import-orders">打开批量导入</a></div></div></section><section class="panel"><div class="panel-head"><div><h2>数据管理</h2><p>危险操作只对主管开放</p></div></div><div class="panel-body"><div class="boundary-note">“清空业务数据”会删除订单、任务、消息和审计记录。仅在测试环境使用。</div><div class="button-row" style="margin-top:14px"><button class="btn" id="seedDemo">加载可删除的示例数据</button><button class="btn danger" id="clearData">清空业务数据</button></div></div></section>`;
-  }else{
-    managerContent=`<section class="panel"><div class="panel-head"><div><h2>当前权限范围</h2><p>工作身份会直接改变可见订单、任务和管理功能</p></div></div><div class="panel-body"><div class="permission-card"><strong>${esc(operatorNames[currentUser()]||currentUser())} · 跟单人员</strong><ul><li>订单中心只显示负责人为我的订单</li><li>今日行动和全部任务只显示分配给我的任务</li><li>新建或导入未填写负责人的订单，默认绑定当前操作人</li><li>团队负载、系统连接和数据清理仅主管可见</li></ul></div><div class="boundary-note">当前身份切换用于求职演示，不等同于正式账号登录。商业版本还需要企业账号、权限认证和操作授权。</div></div></section>`;
-  }
-  root.innerHTML=`<div class="page-stack"><div class="page-toolbar"><div><h2>${isManager?'主管设置':'我的身份与提醒'}</h2><p>${isManager?'查看团队权限、系统连接和数据管理':'当前页面只保留与你工作相关的身份和提醒设置'}</p></div><button class="btn primary" id="saveSettings">保存设置</button></div><section class="settings-grid"><section class="panel"><div class="panel-head"><div><h2>演示工作身份</h2><p>切换后，订单、任务和管理入口会按身份重新加载</p></div></div><div class="panel-body page-stack"><label class="field"><span>当前操作人</span><select id="currentUserSelect">${ops.items.map(o=>`<option value="${esc(o.user_id)}" ${o.user_id===currentUser()?'selected':''}>${esc(o.name)} · ${esc(o.role)}</option>`).join('')}</select></label><div class="setting-list"><div class="setting-row"><div class="setting-copy"><strong>紧急任务提醒</strong><small>显示已逾期、高风险和需要升级的事项。</small></div><label class="switch"><input id="notifyUrgent" type="checkbox" ${settings.notifications?.urgent!==false?'checked':''}><i></i></label></div><div class="setting-row"><div class="setting-copy"><strong>等待超时提醒</strong><small>承诺回复时间超过后，任务重新进入行动队列。</small></div><label class="switch"><input id="notifyWaiting" type="checkbox" ${settings.notifications?.waiting_overdue!==false?'checked':''}><i></i></label></div><div class="setting-row"><div class="setting-copy"><strong>写回结果提醒</strong><small>人工确认并写回后展示结果和审计状态。</small></div><label class="switch"><input id="notifyWriteback" type="checkbox" ${settings.notifications?.writeback!==false?'checked':''}><i></i></label></div></div></div></section>${managerContent}</section></div>`;
-  $('#saveSettings',root).onclick=async()=>{const user=$('#currentUserSelect',root).value;const newSettings={...settings,current_user_id:user,theme:'upstream',show_demo:false,notifications:{urgent:$('#notifyUrgent',root).checked,waiting_overdue:$('#notifyWaiting',root).checked,writeback:$('#notifyWriteback',root).checked,daily_summary:false}};try{await api(`/api/settings?user_id=${encodeURIComponent(user)}`,{method:'PUT',body:JSON.stringify({settings:newSettings})});localSet('currentUserId',user);settings=newSettings;cache={operators:cache.operators};updateProfile();toast('身份与设置已保存，页面已按新权限重新加载','success');renderRoute(false)}catch(e){toast(e.message,'error')}};
-  $('#communicationKey',root)?.addEventListener('click',openCommunicationKeyModal);
+  const statusRows=`<div class="run-row"><span class="r-label">系统服务</span><span class="r-val">在线</span></div>
+    <div class="run-row"><span class="r-label">Agent 服务</span><span class="r-val">${agentStatus.coze_agent?.configured?'Coze Agent 已配置':'未配置，规则巡检仍可使用'}</span></div>
+    <div class="run-row"><span class="r-label">定时巡检</span><span class="r-val">${agentStatus.daily_schedule?.scheduler_verified?'外部调度已验证':agentStatus.daily_schedule?.configured?'接口与密钥已配置，外部调度待验证':'未配置'}</span></div>
+    <div class="run-row"><span class="r-label">数据持久化</span><span class="r-val">${storage.on_persistent_path?'已启用':'待检查'}${storage.db_path?` · ${esc(storage.db_path)}`:''}</span></div>`;
+  root.innerHTML=`<div class="page-stack">
+    <section class="panel"><div class="panel-head"><h3>身份与权限（演示模式）</h3><span class="tag muted">演示</span></div><div class="panel-body">
+      <div class="row-actions" style="margin-bottom:14px"><select class="btn secondary" id="currentUserSelect">${ops.items.map(o=>`<option value="${esc(o.user_id)}" ${o.user_id===currentUser()?'selected':''}>${esc(o.name)} · ${esc(o.role)}</option>`).join('')}</select><button class="btn primary" id="saveSettings">保存身份</button></div>
+      <p class="demo-note">切换身份只用于当前MVP演示；跟单专员查看本人订单，主管查看团队数据。</p>
+    </div></section>
+    <h4 class="section-title">系统状态</h4>
+    <div class="proto-2col">
+      <section class="panel"><div class="panel-head"><h3>订单底座与连接</h3><span class="tag ${importStatus.supported_formats?'success':'warning'}">${importStatus.supported_formats?'可用':'待检查'}</span></div><div class="panel-body">
+        <div class="run-row"><span class="r-label">批量导入</span><span class="r-val">${importStatus.supported_formats?`${esc(importStatus.supported_formats.join(' / '))} · 最多${Number(importStatus.max_rows||0)}行`:'状态获取失败'}</span></div>
+        <div class="run-row"><span class="r-label">订单识别</span><span class="r-val">${cozeStatus.ready?'核心自动化已配置':'部分能力待配置'}</span></div>
+        <div class="row-actions" style="margin-top:12px"><a class="btn primary" href="/import-orders">导入订单底座</a>${importStatus.supported_formats?'<a class="btn secondary" href="/api/import/template.xlsx">下载Excel模板</a>':''}</div>
+      </div></section>
+      <section class="panel"><div class="panel-head"><h3>Agent 与巡检状态</h3><span class="tag ${agentStatus.coze_agent?.configured?'success':'warning'}">${agentStatus.coze_agent?.configured?'已配置':'部分待配置'}</span></div><div class="panel-body">${statusRows}<div class="row-actions" style="margin-top:12px"><button class="btn secondary" id="refreshSystemStatus">刷新状态</button></div></div></section>
+    </div>
+    ${isManager?`<section class="panel"><div class="panel-head"><h3>数据管理</h3><span class="tag danger">仅测试环境</span></div><div class="panel-body"><p class="demo-note">加载示例数据或清空业务数据只用于项目演示，不属于日常跟单操作。</p><div class="row-actions" style="margin-top:12px"><button class="btn secondary" id="seedDemo">加载示例数据</button><button class="btn danger" id="clearData">清空业务数据</button></div></div></section>`:''}
+  </div>`;
+  $('#saveSettings',root).onclick=async()=>{const user=$('#currentUserSelect',root).value;const newSettings={...settings,current_user_id:user,theme:'upstream',show_demo:false,notifications:settings.notifications||{}};try{await api(`/api/settings?user_id=${encodeURIComponent(user)}`,{method:'PUT',body:JSON.stringify({settings:newSettings})});localSet('currentUserId',user);settings=newSettings;cache={operators:cache.operators};updateProfile();toast('身份已切换，页面将按权限重新加载','success');go('today')}catch(e){toast(e.message,'error')}};
+  $('#refreshSystemStatus',root).onclick=()=>renderRoute(false);
   $('#seedDemo',root)?.addEventListener('click',async()=>{if(!confirm('加载示例数据会覆盖当前业务数据，确定继续吗？'))return;try{await api('/api/demo/seed',{method:'POST',body:'{}'});cache={operators:cache.operators};toast('已加载示例数据','success');go('today')}catch(e){toast(e.message,'error')}});
   $('#clearData',root)?.addEventListener('click',async()=>{if(!confirm('此操作会删除全部业务数据，且无法撤销。确定继续吗？'))return;try{await api('/api/reset',{method:'POST',body:'{}'});cache={operators:cache.operators};toast('业务数据已清空','success');go('today')}catch(e){toast(e.message,'error')}});
 }
