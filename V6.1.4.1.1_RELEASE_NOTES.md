# FlowOrder V6.1.4.1.1 部署健康检查热修复

## 问题
Railway 构建与容器启动完成，但 Network / Healthcheck 在约 5 分钟后失败。

V6.1.4.1 的 `/health` 会再次执行数据库初始化、Schema 脚本和状态聚合；同时源码还在模块导入和 startup 阶段重复执行 `init_db()`。这使部署健康检查依赖 SQLite 持久卷、迁移和锁状态，任何瞬时问题都会使 `/health` 无法及时返回 HTTP 200。

## 修复
- `/health` 改为纯存活检查，不执行数据库迁移、不访问 Coze、不读取业务数据。
- 新增 `/ready`，用于确认 SQLite 已完成初始化并可查询。
- 数据库初始化移到后台线程，避免阻塞 Railway 网络健康检查。
- 数据库初始化增加 6 次退避重试，并记录可见错误。
- SQLite 连接增加 30 秒 timeout 和 busy_timeout。
- 兼容旧版 `intake_jobs` 表结构，缺列时跳过陈旧任务清理而不是使服务启动失败。
- 删除模块末尾的第三次 `init_db()`。
- Dockerfile 和 Railway 启动命令统一监听 `${PORT:-8000}`。
- Railway 健康检查超时从 300 秒收紧至 60 秒。

## 未修改
- Agent 路由、FT04 共享排序、多目标执行计划。
- 数据库正式业务数据。
- `static/styles.css`、页面颜色、字体、卡片和布局。
