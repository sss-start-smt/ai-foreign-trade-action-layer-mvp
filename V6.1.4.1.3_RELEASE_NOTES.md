# V6.1.4.1.3

- 新增不依赖业务模块的Railway安全启动分发器。
- `/health`在端口绑定后立即可用。
- 新增`/bootstrap-status`显示`main.py`导入错误和完整traceback。
- 业务应用加载完成后，所有原路由透明转发到原FastAPI应用。
- 数值环境变量改为容错解析，空值或非数字不再导致进程启动前退出。
- 修复旧`intake_jobs`兼容分支中的`required-columns`拼写错误。
- 显式指定Railway使用Dockerfile构建。
- 不修改前端视觉与业务数据库结构。
