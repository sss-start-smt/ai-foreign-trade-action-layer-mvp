# FlowOrder V6.1.4.1 Release Notes

## 目标

把Agent从“固定句式/关键词命中后调用Coze”升级为：

- 混合意图路由；
- 多目标任务拆解；
- 标准风险诊断走确定性快速路径；
- Agent诊断与今日工作台共用FT04行动规则；
- 开放解释和沟通生成才交给Coze；
- 正式动作继续人工确认。

## 代码变化

### 新增 `agent_router.py`

- 识别口语化、冗长表达；
- 提取未来天数、Top N、目标排名、订单号；
- 识别风险诊断、解释排序、批量更新、任务草稿、沟通草稿、审批查询；
- 支持一句话多个目标及依赖关系；
- 提取“不要发送、不要写回、不要建任务、只生成草稿”等限制；
- 模糊目标返回最小澄清问题；
- 路由器不负责权限提升和正式执行。

### 新增 `action_rules.py`

把原 `main.py` 中的FT04任务行动状态与优先级规则抽为共享规则：

- 等待承诺窗口；
- 等待超时恢复；
- 截止时间；
- 高风险与升级；
- 待人工确认；
- 责任人；
- 周末非紧急抑制；
- `priority_score`与`priority_reasons`。

今日工作台和Agent订单诊断调用同一函数。

### Agent执行器

- `RISK_DIAGNOSIS`：直接调用 `diagnose_priority_orders`；
- `EXPLAIN_PRIORITY`：基于本次或上次结构化诊断解释；
- `CREATE_TASK_DRAFT`：仅在用户明确要求时创建草稿和可选审批；
- `BATCH_UPDATE_PARSE`：调用批量进展解析，只生成候选；
- 批量候选未确认时，不使用这些未确认数据继续风险排序；
- 未识别清楚时不猜测执行；
- 其他开放目标进入Coze，最长等待60秒。

### Agent短期续聊

- 前端发送 `previous_run_id`；
- “为什么第一笔”“刚才那单”等问题可读取上一轮结构化结果；
- 补齐 `pollConversationJob`；
- 完成后回填回答、`run_id`、`conversation_id`、指标、草稿ID和审批ID。

### Coze文件

- 更新12工具插件描述，降低工具误选；
- 新增正式网站用6工具精简插件；
- 更新V6.1提示词，加入 `route_plan`、多意图、限制条件和工具边界。

## 安全边界

- 路由器不扩大权限；
- 风险排序不由LLM自由打分；
- 信息缺口不进入风险Top N；
- 批量进展不直接写回；
- 任务只生成草稿；
- 沟通草稿不自动发送；
- 正式订单变更和高风险动作继续人工审批。

## 验证

- Python语法检查：通过；
- JavaScript语法检查：通过；
- 自动化测试：56项通过；
- 本地标准多意图链路：约0.05秒完成（仅为本地测试，不代表线上固定耗时）；
- `static/styles.css` SHA-256：`479dbac3eb133c74a1e6134e224c627356579a3de135ff3d70bdde72eb3e3a23`，未修改。
